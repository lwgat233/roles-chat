[2026-09-25 09:16:28] owner.me → pipeline.renderer | 全体? 否 | 话题:【派活】hermes-pocket-R40 第 4 步 修复口径（女仆标签/通道不算已废/退化态） | 必读:是 | #1304
    接 hermes-pocket-R40 **第 4 步「修复口径」**（测试者 R40-3 真机复测抓到的：女仆那条显示 `#4 可爱女仆（已废）`，与口径不符）。**只改 3 处口径，顺手动别的**。
    
    测试者读数（原文在 `evidence/R40-复测-20260925/`）：会话页 8 行、每行 `#<id> <身份>`、分组「在跑(5)/没在跑(1)/临时对话(2)」都出来了 ✓；同号同人 ✓；没会话显「未起会话」不显号 ✓；**不达标的只有女仆那条**（它把 `title` 去括号当身份 + 按 `alive` 判「（已废）」）。
    
    **口径（我拍的，按这个改）**：
    1) **女仆那条（`session#4`）固定显 `#4 女仆（本人通道）`** —— 身份用**固定标签**，**不要**取平台 `title`/`name`（`可爱女仆（奈奈）` 可留作副显示或不显）；卡片副行/信息窗那行也要**带身份**（现在只有窗口名 `#4 · 本人-女仆通道`，要成 `#4 · 女仆（本人通道）`）。
    2) **「（已废）」判据要分清形态**：只对 **`会话:窗口` 形态**的 `tmux` 用 tmux 判活并标「（已废）」；像 `qqbot:3FDE0CB3…` 这种**通道目标不参与"已废"判定**，标「**QQ 通道**」且**恒在线**（平台侧 `role_online('home.maid')` 恒 True —— QQ 通道在＝她在）。**不许**因为"tmux 里没这个会话"就把女仆标成已废。
    3) **加载顺序的退化态**：roles 还没到位时**只显 `#<id>（加载中）`**，**不许**把平台 `name`（`pipeline.tester` 这种）当身份显出来；roles 到位后**必须替换**为 `#5 测试者`（不许停在退化态）。
    4) 除这 3 处**不动别的**：编号来源仍是 `talk.sessions[].id`、去重仍按 `tmux` 全名、容器不成行、分组逻辑、点「删」按人（不按容器）都保持。
    
    出包：`bash tools/build.sh --name "R40-会话编号修正" --feature-id "R-40"`；交付 NEWS（≤8 行）：产物路径 + 整包 sha256 + 包内 build-info + **你本次改动文件的 sha** + 三处读数（女仆那条现在显示什么、`qqbot:` 形态标什么、退化态显示什么）。
    口径：不接主机、**未验 ≠ 通过**（真机复测归 tester 第 5 步）。
    ↳ 回复 [2026-09-25 09:18:37] pipeline.renderer: 改完了，报告 #(hermes-pocket-R40#4 已记)。三处读数：① 女仆那条＝会话页 （在跑组、右标 QQ 通道、不标已废）、弹窗 、卡片副行 、信息窗会话行同；② qqbot: 形态右标「QQ 通道」、恒在线（落「在跑」组）；③ roles 未到位只显 （平台 name 一个没露），到位后替换成 。我做时发现并挡掉一个连带误判：裸 tmux 名（老废条目 role-pipeline-tester/solo-… 没冒号）第一版会跟着当通道、把  错显成女仆——现在只有「有冒号 + 窗口段不是可读窗口名」才算通道，#3 仍正确标（已废）。产物 apk/测试版/hermes-pocket-R40-会话编号修正-20260925.apk 整包 sha256 330e153c…，talk.js e75baa5a…、panels.js 47702ea6…（包内=源树逐字节），build-info unified-20260925-091752，整树 --verify PASS。除这 3 处没动别的（编号来源/去重/容器不成行/分组/点删按人 都保持）。证据 evidence/R40-修复口径-20260925/。等 gate-done → tester 第 5 步真机。
    ↳ 回复 [2026-09-25 09:18:40] pipeline.renderer: 更正一句：报告号是 **#1305**（hermes-pocket-R40#4），我上一条写成「#(…)」是笔误；其余内容不变。

[2026-09-25 09:26:07] owner.me → pipeline.renderer | 全体? 否 | 话题:【派活】hermes-pocket-R41 第 2 步 界面改动与出包（回车=发送 A 案） | 必读:是 | #1319
    接 hermes-pocket-R41 第 2 步「界面改动与出包」。作者定位 #1315 已判过（机制、事件序列、候选接线读数都在 `evidence/R41-定位-20260925/`，别重读大文件）。
    
    **换行那条我拍了：走 A 案**（保持单行 `<input>`，三条兜底都带 `!shiftKey`，Shift+Enter 不发送也不换行 —— 最小改动、先把这个红修掉）。B 案（`<textarea>` 自动长高）**记为候选**，等本人说要长文本换行再开。
    
    按作者改法改（`assets/ui/talk.js`，只动这几处）：
    1) **`enterkeyhint="send"`** 加三处：群聊 `talk.js:492-494`（`#tk-shoutin`）、单聊 `talk.js:1058-1060`（`#tk-sayin`）、**授权回复框 `talk.js:598-600`（`.tk-askin`）**；
    2) **`keyup` + `keyCode===13` 兜底**：群聊、单聊**各一条**；授权框作者说它**连 Enter 都没挂**→ 顺手把「Enter 发送」挂上（与另外两处同样的兜底口径）；
    3) **`beforeinput` 兜底**：判 `inputType === "insertLineBreak" || "insertParagraph"` 再发一次（**这是唯一能救"完全没有按键事件"的输入法**）；三条兜底**都要 `!shiftKey`**，且**不要在 `compositionend` 之前抢发**（组字中不许发送）；
    4) **防重复发送（硬要求）**：每个输入框一个 `sentAt` 时间窗（**400ms**）＋ 发完清空 value；判据＝**同一次回车只发一条**（三条路同时触发也只 1 条），但**连按两次仍应发两条**（别把用户的第二次真按键吃掉）；
    5) **不许顺手动别的**：投递侧（`Bridge.kt` / `deliver()`）作者已复核没问题，**不碰**；草稿、群聊那行的既有行为、`.tk-askin` 的样式都不改。
    
    出包：`bash tools/build.sh --name "R41-回车发送" --feature-id "R-41"`；交付 NEWS（≤8 行）：产物路径 + 整包 sha256 + 包内 build-info + **你本次改动文件的 sha** + 四条读数（三处 `enterkeyhint` 到位、两处+授权框的兜底、同一次回车只发一条、Shift+Enter 不误发）。
    口径：不接主机、**未验 ≠ 通过**（真机多输入法复测归 tester 第 3 步；判据＝**真机按输入法回车能发出**（App 出现自己气泡 + 对方会话条数 +1）且**同一次回车不出两条**）。
    ↳ 回复 [2026-09-25 09:28:46] pipeline.renderer: 做完了，报告 #(见下)。做了什么：只动 talk.js —— 新增 bindEnterSend（enterkeyhint=send + keydown 主路 + keyup(13)/beforeinput(insertLineBreak|insertParagraph) 兜底 + 400ms 一把闸 + Shift+Enter 不发 + 组字中不发），接在群聊 #tk-shoutin、单聊 #tk-sayin、授权框 .tk-askin（原来连 Enter 都没挂，抽成 reply() 一并挂上）；投递侧/Bridge.kt、草稿、群聊那行、样式、R-31/R-36 都没碰，B 案记候选。四条读数：①三处 enterkeyhint=send；②只给 keyup(13) 群聊发1条(清空)、只给 beforeinput 单聊发1条、输入法提交形状(无 keydown)发1条；③一次完整回车三路全触发只发1条（单聊/群聊/授权框 talk.answer 各 1）、连按两次(间隔600ms)发2条；④Shift+Enter 0 条且文字留着；组字中 0 条；报错 0。产物 apk/测试版/hermes-pocket-R41-回车发送-20260925.apk 整包 sha256 93425743…，ui/talk.js ba48cc02…（包内=源树逐字节），build-info unified-20260925-092803，整树 --verify PASS。一句如实说明：连按两次的判据我按 600ms 间隔测（R-31 的防连点仍在，属既有行为没动）；要让「上一条还在发时」连按也发两条，请再给口径。证据 evidence/R41-实现与出包-20260925/。
    ↳ 回复 [2026-09-25 09:28:49] pipeline.renderer: 补一句（我上一条报告号写漏了）：报告号是 **#1320**（hermes-pocket-R41#2）。

[2026-09-25 09:31:22] owner.me → pipeline.renderer | 全体? 否 | 话题:【⑥讨论】输入条口径（R-41/R-42/R-44） | 必读:是 | #1326
    【⑥讨论】输入条口径（R-41 / R-42 / R-44）—— 你的一份，写进文件。
    
    **由来**：本人 2026-09-24 当场说「我在频道里沟通的时候，那个输入框一直保留在那里，就不太合理。如果你保留在那里，那按道理说……我可以使用它，把这段话发送给频道里的某个角色」→ **倾向 A，但不拍板**，原话「明天让他们自己去讨论哪个更加合理」。所以今天由 author / renderer / tester 各自出理由+证据，经理写决定书记票（本人回来可推翻）。
    
    **唯一交付 = 一个文件**（≤1 页，四段，缺一段不算交）：
    路径 `/vol1/1000/airesults/hermes-pocket/docs/讨论-输入条口径-renderer.md`
     1. 候选 **A**（不该出现的场景就不保留输入条）的**代价与风险**；
     2. 候选 **B**（保留，但必须真能用：能指定发给频道里的某个角色）的**代价与风险**；
     3. **证据**（≥2 条，要能被复核：`文件:行`、真机读数、或既有报告编号）；
     4. **你投哪一票**（A 或 B）＋一句话理由。
    
    **对话里只回一句**：`文件路径 + 一句话结论`。大段内容走文件（省 token），别在对话里复述文件。
    
    **边界（硬）**：不改代码、不开阶段、不打包、不接主机；R-41 不用重挖（已有 #1315 定位 → #1320 实现出包 → #1324 复测已派）。讨论**一次说清同一片输入区**：R-41（回车不能确认/发送）、R-42（频道页点开不能聊天）、R-44（那条给 tmux 用的输入条）是同一件事的三个症状。
    
    **可查的现成材料**（按需查，别整读）：`evidence/R41-定位-20260925/`、`evidence/R41-实现与出包-20260925/`、`evidence/R41-复测-20260925/`、`evidence/输入与发送栏/`、`docs/界面渲染规范.md`、登记表 R-41/R-42/R-44 行。
    
    **你的角度**：
    （渲染者）从**界面形态/渲染规范**判 —— 那条输入条由谁渲染、在哪些页面出现（会话页 / 频道页 / 其它），A 与 B 各要改哪些渲染面、重出包的代价；B 案"指定收件人"在常驻按键 ≤3、不写说明文字的铁规下怎么摆。
    
    **交完等**：经理（owner.me）写 `roles-chat/docs/决定-2026-09-25-输入条口径.md`，三人一人一票、经理只记票署名。

[2026-09-25 09:32:39] owner.me → pipeline.renderer | 全体? 否 | 话题:【告知·⑥讨论】输入条口径（R-41/R-42/R-44） | 必读:是 | #1329
    【线路说明】本条按平台的**转告类（【告知）**路线送进来：你的阶段闸门锁着，平台只放不是派活的转告进来。
    这**不是派活、不许开阶段、不许改代码**，就是要你交那一页讨论文件；收到照下面做。
    
    【⑥讨论】输入条口径（R-41 / R-42 / R-44）—— 你的一份，写进文件。
    
    **由来**：本人 2026-09-24 当场说「我在频道里沟通的时候，那个输入框一直保留在那里，就不太合理。如果你保留在那里，那按道理说……我可以使用它，把这段话发送给频道里的某个角色」→ **倾向 A，但不拍板**，原话「明天让他们自己去讨论哪个更加合理」。所以今天由 author / renderer / tester 各自出理由+证据，经理写决定书记票（本人回来可推翻）。
    
    **唯一交付 = 一个文件**（≤1 页，四段，缺一段不算交）：
    路径 `/vol1/1000/airesults/hermes-pocket/docs/讨论-输入条口径-renderer.md`
     1. 候选 **A**（不该出现的场景就不保留输入条）的**代价与风险**；
     2. 候选 **B**（保留，但必须真能用：能指定发给频道里的某个角色）的**代价与风险**；
     3. **证据**（≥2 条，要能被复核：`文件:行`、真机读数、或既有报告编号）；
     4. **你投哪一票**（A 或 B）＋一句话理由。
    
    **对话里只回一句**：`文件路径 + 一句话结论`。大段内容走文件（省 token），别在对话里复述文件。
    
    **边界（硬）**：不改代码、不开阶段、不打包、不接主机；R-41 不用重挖（已有 #1315 定位 → #1320 实现出包 → #1324 复测已派）。讨论**一次说清同一片输入区**：R-41（回车不能确认/发送）、R-42（频道页点开不能聊天）、R-44（那条给 tmux 用的输入条）是同一件事的三个症状。
    
    **可查的现成材料**（按需查，别整读）：`evidence/R41-定位-20260925/`、`evidence/R41-实现与出包-20260925/`、`evidence/R41-复测-20260925/`、`evidence/输入与发送栏/`、`docs/界面渲染规范.md`、登记表 R-41/R-42/R-44 行。
    
    **你的角度**：
    （渲染者）从**界面形态/渲染规范**判 —— 那条输入条由谁渲染、在哪些页面出现（会话页 / 频道页 / 其它），A 与 B 各要改哪些渲染面、重出包的代价；B 案"指定收件人"在常驻按键 ≤3、不写说明文字的铁规下怎么摆。
    
    **交完等**：经理（owner.me）写 `roles-chat/docs/决定-2026-09-25-输入条口径.md`，三人一人一票、经理只记票署名。
    ↳ 回复 [2026-09-25 09:33:32] pipeline.renderer: /vol1/1000/airesults/hermes-pocket/docs/讨论-输入条口径-renderer.md —— 我投 A（不该出现的场景不保留输入条）：那条 #composer 的语义是「写进 tmux 终端」（app.js:653），放频道页只会让人误以为能聊天，而「发给频道里某个角色」这条路已由单聊输入条提供（talk.js:1095-1105，收件人＝你点开的会话），A 只动 app.js 一处显隐＋一次 resize 复验；B 要在同一个框里混装两种投递语义，直接撞「常驻键 ≤3 / 不写说明文字」两条铁规（界面渲染规范 §37/§39）。四段齐全（A 代价风险 / B 代价风险 / 6 条可复核证据 / 投票＋理由）。未改代码、未开阶段、未打包。

[2026-09-25 09:36:28] owner.me → pipeline.renderer | 全体? 否 | 话题:【派活】hermes-pocket-R41 第 4 步 修复（组字守卫吞回车） | 必读:是 | #1335
    接 hermes-pocket-R41 **第 4 步「修复」（真机红：回车在单聊页 0 次，群聊时通时不通）**。测试者 R41-3 真机定位（证据 `evidence/R41-复测-20260925/`）：**是你那套「组字中不发」的守卫把整条路吞了** —— Gboard 在这个框里会 `compositionstart` 而**从不送 `compositionend`** ⇒ `composing` 永远为真 ⇒ Enter 永久失效；群聊"有时通"就是偶发没有组字态的那次。
    
    **口径（我拍的，按这个改）**：
    1) **Enter 的放行条件改成**：`e.key === 'Enter' && !e.shiftKey` 就**发送**；**只拦 `keyCode === 229`**（那才是真正的 IME 合成中间态）；**`isComposing === true` 不再单独拦**（实测 Gboard 的 Enter 是 `isComposing=true` + `keyCode=13`，那是"确认/上屏并发送"，正是本人要的行为）。
    2) **组字态必须有兜底退出**（不许再出现"永久 composing"）：`compositionstart` 后启动 **1.5s 定时器**，到点无条件 `composing = false`；`compositionend` 照旧立即复位；两者谁先到算谁。
    3) **保留**：`keyup(13)`/`beforeinput(insertLineBreak|insertParagraph)` 两条兜底、**400ms 一把闸**、值非空才发、发完清空、Shift+Enter 不发送且文字留着。
    4) **授权框 `.tk-askin` 同上**（它也是同一个 `bindEnterSend`）。
    5) 投递侧不碰。
    
    **修完自验（本地台）要给的读数**：① 模拟"Gboard 形状"（`compositionstart` → `keydown(Enter,13,isComposing=true)` → `beforeinput(insertLineBreak)` → `keyup`，**不给 compositionend**）→ **必须发 1 条**；② 组字态残留 1.5s 后自动复位（给复位前后的 `composing` 读数）；③ 同一次回车仍**只 1 条**、600ms 连按 2 条；④ Shift+Enter 0 条且文字留着。
    
    出包：`bash tools/build.sh --name "R41-回车发送修正" --feature-id "R-41"`；交付 NEWS（≤8 行）：产物路径 + 整包 sha256 + 包内 build-info + **你本次改动文件的 sha** + 上面四条读数。
    口径：不接主机、**未验 ≠ 通过**（真机 Gboard 复测归 tester 第 5 步；**判据＝真机按回车 → App 出现自己气泡 + 对方会话条数 +1**）。
    ↳ 回复 [2026-09-25 09:38:36] pipeline.renderer: 改完了，报告号见下一条。根因我认下：老守卫  被 Gboard 顶死（只送 compositionstart、不送 compositionend ⇒ composing 永不复位 ⇒ 单聊回车永久失效，群聊偶尔通＝恰好没组字态）。改法按你的口径：① 放行条件  即发送、**只拦 keyCode===229**、isComposing 不再单独拦；② compositionstart 后 1.5s 无条件兜底复位（compositionend 立即复位，谁先到算谁）；③ 保留 keyup(13)/beforeinput 兜底 + 400ms 闸 + 非空才发 + 发完清空 + Shift+Enter 不发；④ 授权框同一入口（bindEnterSend）一并生效；⑤ 投递侧没碰。四条读数：① Gboard 形状（compositionstart→keydown(Enter,13,isComposing=true)→beforeinput→keyup，无 compositionend）→ 群聊 1 条且清空、单聊 1 条、授权框 1 条（改前真机都是 0）；② 组字态复位前 {composing:true,timer:true}→1.8s 后 {false,false}；③ keyCode=229 → 0 条、随后正常回车 1 条；④ 一次回车 1 条、600ms 连按 2 条、Shift+Enter 0 条且文字留着；报错 0。产物 apk/测试版/hermes-pocket-R41-回车发送修正-20260925.apk 整包 sha256 99137db7…，ui/talk.js dcc3a9a7…（包内=源树逐字节），build-info unified-20260925-093752，整树 --verify PASS。证据 evidence/R41-修复-20260925/。
    ↳ 回复 [2026-09-25 09:38:46] pipeline.renderer: 补一处修正（我上一条回复里有两段代码被我这边的命令转义吃掉了，读起来缺词）：被 Gboard 顶死的那句守卫原文是 `if (composing || e.isComposing) return;`；本次改后的放行条件是 `key === "Enter" && !shiftKey` 即发送（只拦 `keyCode === 229`）。其余内容与读数不变；报告号 **#1336**（hermes-pocket-R41#4）。

[2026-09-25 09:48:16] owner.me → pipeline.renderer | 全体? 否 | 话题:【⑥讨论·结论】输入条口径 → B 案（保留但必须真能用） | 必读:否 | #1351
    【⑥讨论·结论已出】输入条口径 → 决定书 `roles-chat/docs/决定-2026-09-25-输入条口径.md`
    票数：B 两票（author、tester）／ A 一票（renderer）⇒ 结论走 B：保留该输入条，但必须先做成「收件人显式」的真输入条（可见收件人状态 + 走 talk.say + 与实时模式互斥），否则不予保留。
    生效范围：频道页本片输入区 + 其它「非 tmux 会话页」场景同口径；不影响 R-41（回车已修）与 R-40（编号身份）。
    执行约束（renderer 的反对意见已记进决定书，落实现必须守）：不许破坏「常驻键 ≤3」「不写说明文字」；不许新增第二条同义输入通道；实现前先给判据。
    排期：R-42 的 ①（卡片一步进单聊）先做（已派），B 案的「频道页自带输入条」排在它之后。这一轮不用回、不用动。

[2026-09-25 09:48:47] owner.me → pipeline.renderer | 全体? 否 | 话题:【派活】hermes-pocket-R42 第 2 步 界面改动与出包（卡片一步进单聊） | 必读:是 | #1354
    接 hermes-pocket-R42 第 2 步「界面改动与出包」。作者定位 #1346 已判过（三条入口的真实行为 + 改法 + 冲突检查都在 `evidence/R42-定位-20260925/`，别重读大文件）。
    
    **口径（我拍的）：只做作者改法的 ①，先不做 ② ③。**
    - **① 角色卡单击 = 直接进单聊**：`talk.js:637` 的 `openRoleSheet(r.full_name)` → **`openRole(r.full_name)`**；**信息窗入口挪到卡片右侧箭头 `.tk-caret`（`talk.js:620-624`）**，箭头热区**必须 ≥44dp**（照 R-33 硬线；`.tk-chrow` 现状 36px 也要顺手补到 ≥44px）。
    - **明确不做（这两条等别的口径）**：②「频道（N）」那行的点击/标注 —— 归 **R-44 决定书**（已出：走 B 案保留但做成能用的，排在 R-42 之后）；③ 频道页自带"跟谁说"输入条 —— 同一份决定书，**不许在这步顺手加**。
    
    **冲突检查（做的时候要守住）**：
    - **R-37**：单聊页「点抬头开信息窗」**必须仍然好使**（我们只是把"卡片"那层的入口换掉，不动单聊页）；
    - **R-40**：卡片副行的 `#4 · 女仆（本人通道）` 等编号显示**不受影响**（实测已有）；但"点卡片看信息窗"这条入口变了 ⇒ 报告里写明"**入口改为箭头**，给 R-40 的复测口径用"；
    - **R-33**：新增箭头部热区 ≥44dp。
    
    出包：`bash tools/build.sh --name "R42-频道页一步进单聊" --feature-id "R-42"`；交付 NEWS（≤8 行）：产物路径 + 整包 sha256 + 包内 build-info + **你本次改动文件的 sha** + 读数（点卡片几步进单聊、箭头热区 px/dp、点头部开信息窗仍好使）。
    口径：不接主机、**未验 ≠ 通过**（真机复测归 tester 第 3 步；判据＝**从频道页一步进到能给人发消息**、发出后 App 出自己气泡）。
    ↳ 回复 [2026-09-25 09:50:47] pipeline.renderer: 做完了，报告号见下一条。只做①：talk.js roleCard 的卡片单击 openRoleSheet→openRole（一步进单聊），信息窗入口挪到卡片右缘 44px 宽透明热区（`<span class="tk-carethit" data-testid="talk-rolecaret">`，absolute 不撑高卡片，箭头符号 › 保持原样）；talk.css 新增 .tk-carethit、.tk-chrow 36→44px（照 R-33）。②③ 按口径没做（归 R-44 决定书），单聊页/R-31/R-36/R-26 都没碰。
    读数：① 点卡片名称处 **1 步**进单聊（单聊页 + 发送键都在、talk.thread 1 次、信息窗没弹）；② 箭头热区 44×78px（卡高 80px），四角+中心 elementFromPoint 全部命中热区自己；③ 单聊页点抬头开信息窗仍好使（R-37 回归）；④ 点箭头开窗且仍留在频道页；⑤ 卡片副行 `#6 · owner-me` 不变（R-40 不受影响）；⑥ .tk-chrow 2 行均 44px；⑦ 报错 0。
    **给你的 R-40 复测口径**：角色信息窗入口从"点卡片"改成"点卡片右缘箭头（44px 宽热区）"，编号/身份显示口径没变。
    产物 apk/测试版/hermes-pocket-R42-频道页一步进单聊-20260925.apk 整包 sha256 92aaeb24…，ui/talk.js db4d5de9…、ui/talk.css e1503184…（包内=源树逐字节），build-info unified-20260925-094957，整树 --verify PASS。证据 evidence/R42-实现与出包-20260925/。
    ↳ 回复 [2026-09-25 09:50:50] pipeline.renderer: 补一句：报告号是 #1357（hermes-pocket-R42#2）。

[2026-09-25 10:13:12] owner.me → pipeline.renderer | 全体? 否 | 话题:【派活】hermes-pocket-R39 第 2 步 界面改动与出包（末条不被 sticky 条压住） | 必读:是 | #1389
    接 hermes-pocket-R39 第 2 步「界面改动与出包」。作者定位 #1385 已判过（根因、甲/乙对照、读数都在 `evidence/R39-定位-20260925/`，别重读大文件）。
    
    **口径（我拍的）：走乙案 + 顺手做成"共用规则"。**
    1) **主改**：给**内层滚动容器 `#tk-chat`** 补 `padding-bottom`（作者实测：`calc(68px + var(--safe-b))` ⇒ 末条下沿 577→509、**重叠 0**、中心命中气泡本身）；落点 `talk.js:1040-1048` 的内联样式，或移到 `talk.css` 的 `.tk-chat`（你选更干净的）；**别**去改外层 `.panel-body`（甲案实测重叠变 93px、更糟）。
    2) **别写死数字**：按作者建议把 sticky 条高做成 CSS 变量（`--sayline-h`，`paintRole` 里测一次写 `:root`），`padding-bottom` 用 `calc(var(--sayline-h) + var(--safe-b) + 8px)` 这类**跟着条高走**的写法 —— 字号/安全区一变就不会失配。
    3) **共用规则**（经理口径，已写进 SPEC §10）：**凡是"粘性/固定底部条"，上方滚动内容必须留 `calc(条高 + var(--safe-b))` 的底部内边距**；⑥讨论 B 案（频道页自带输入条）落地时照同一条来。
    4) **不许顺手动别的**：R-38（频道页那条已判"非缺陷"）、R-42 的卡片/右缘热区、R-31/R-36 状态机都不碰。
    
    出包：`bash tools/build.sh --name "R39-末条不被压" --feature-id "R-39"`；交付 NEWS（≤8 行）：产物路径 + 整包 sha256 + 包内 build-info + **你本次改动文件的 sha** + 读数（末条气泡下沿、重叠 px、中心 `elementFromPoint` 命中谁、`#tk-chat` 的 padding-bottom 实际值）。
    口径：不接主机、**未验 ≠ 通过**（真机复测归 tester 第 3 步；判据＝**只滚内层到底后点最后一条气泡能开信息窗** + 三读数）。
