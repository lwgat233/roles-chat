[2026-09-25 09:22:50] owner.me → pipeline.author | 全体? 否 | 话题:【派活】hermes-pocket-R41 第 1 步 定位（App 输入框回车=发送） | 必读:是 | #1314
    接 hermes-pocket-R41 第 1 步「定位与改法」（只定位、只出改法，不写代码）。
    
    **来源**：本人 2026-09-24 当场（App 频道里跟人对话时）「**他无法输入 enter 键表示确认**」。登记表 R-41 有原文与初步定位。
    
    **我已经先查过的（省你时间，直接验证+补全，别重头挖）**：
    - 投递侧没问题：App 走 `Bridge.kt:772 talk.say` → `talk.py say` → 平台 `deliver()`，那条路上有**双回车兜底**（粘完 0.8s 回车 + 0.6s 补一次）＋ O-1 的「对方会话消息条数 +1」确认 ⇒ **不是"字躺在对方输入框"**那类。
    - 输入侧可疑：App 只在 `keydown` 里判 `e.key === 'Enter'`（**群聊 `talk.js:459`**、**单聊 `talk.js:1029`**），而安卓输入法的「回车/发送」键**经常不触发 keydown**（有的只给 `keyCode=229`）；输入框**没设 `enterkeyhint`**（全仓 grep 0 处）。
    
    **要交（≤8 行、新闻格式、¥实价）**：
    1) **量清现状**（文件:行）：两个输入框（群聊 `gin` / 单聊 `inp`）现在各挂了什么事件、`Enter` 走哪条路、**有没有别的输入框漏挂**（频道页/设置页/其它 `talk-askin`）；`Bridge.kt`/`deliver()` 侧有没有二次确认；
    2) **判定**：本地台能不能**复现**"按回车没反应"？（用 CDP 派发**真触摸/真键盘事件**，如果派发 `keydown` 有反应、而派发 `keyCode=229`/`beforeinput` 无反应 ⇒ 就是 IME 那条路）；**不能复现就如实说"本地台复现不了、需真机"**，别硬凑结论；
    3) **改法**（文件:行，四条都要给）：① 输入框加 `enterkeyhint="send"`；② 补 `keyup` + `keyCode===13` 兜底；③ `beforeinput`/`inputType==='insertLineBreak'` 再兜一层；④ 与「长按/Shift 换行」怎么共存（别把换行弄没了）；并说明三条兜底**会不会重复发送**（同一段文字发两次的防护）。
    4) 代价与回归面：要重出的包 + 要复测的板块（照 SPEC §10 点名；判据建议＝**真机按输入法回车能发出消息**（App 出现自己气泡 + 对方会话条数 +1）、换行仍可用）。
    边界：不接主机、**未验 ≠ 通过**；证据进 `evidence/R41-定位-20260925/`；标题【hermes-pocket-R41-1 …】+ 4 行 + 成本¥。
    ↳ 回复 [2026-09-25 09:26:02] pipeline.author: R-41 第 1 步已交报告 #1315（8 行）。
    **先答"能不能复现"**：机制能复现 —— 真 Enter 能发（群聊 shout=1、单聊 say=1），而「输入法提交（insertText：只有 beforeinput/input、没有 keydown）＋只补 keyup」和「只派 beforeinput」**都发不出去（=0）**；事件序：真 Enter＝`keydown(13)→keypress(13)→beforeinput(insertLineBreak)→keyup(13)`，输入法提交＝`beforeinput(insertText)→input(insertText)`（**全程无 keydown**），组字＝`compositionstart→compositionupdate→beforeinput(insertCompositionText,isComposing)→input`。**但真机各输入法到底给哪些事件，本地台给不出答案** —— 我没写"已复现"，只给机制与改法。
    接线现状（你给的初步定位都对，我补全）：群聊 `#tk-shoutin`（talk.js:492-509）与单聊 `#tk-sayin`（talk.js:1058-1083）都**只挂 keydown(Enter)**、**enterkeyhint 全仓 0 处**；**另有一处漏挂**：授权回复框（talk.js:598-620 `.tk-askin`）**连 Enter 都没挂**，只能点发送键；投递侧复核 `Bridge.kt:772 talk.say` → `talk.py:1039 deliver()`（paste-buffer + Enter + 双回车兜底）没问题。
    改法四条（文件:行）：① `enterkeyhint="send"` → talk.js:492-494 / 1058-1060（＋授权框 598-600）；② `keyup` + `keyCode===13` 兜底；③ `beforeinput`/`input` 判 `insertLineBreak|insertParagraph` 兜底（唯一能救"完全没有按键事件"的路）；④ 换行共存两案待拍：**A** 保持单行 input、三条兜底都加 `!shiftKey`（Shift+Enter 不发送也不换行，最小改动）／**B** 换 `<textarea rows=1>`+自动长高（先例 app.js:1892-1893），Enter 发送 / Shift+Enter 真换行，代价＝改动面大。
    **候选接线实测（页面注入、工程未动）**：keyup 兜底→1 次、beforeinput 兜底→1 次、**一次按键三条路都触发→1 次**（防重复生效）、连按两次→2 次；⚠ **抓到 Shift+Enter 被 beforeinput 兜底误发 1 次**（单行 input 没有换行，insertLineBreak 被当成发送）⇒ 每条兜底都必须带 `!shiftKey`。
    防重复：每框一个 `sentAt` 时间窗（建议 400ms）＋发完清空 value（现有 fire() 已清）；⚠ 输入法提交那条路 value 非空，所以时间窗不能省。仓库里已有"两边都接管＝一次回车发两遍（本地实测抓到过）"的先例注释（app.js:1901-1903），出包后请让 tester 专门验一次。
    回归面：改 talk.js ⇒ 必须重出包（归 renderer）；复测板块 主块 ②群聊 + ①单聊（判据＝真机按输入法回车能发出：出现自己气泡＋对方会话条数 +1；换行按 A/B 口径；同一次回车不出两条），跨块 ⑧频道页（授权框也补了）。建议真机至少两种输入法并记下事件序列 —— 这决定 ②③ 两条兜底够不够。
    边界：不接主机、未验≠通过；证据 evidence/R41-定位-20260925/（sha256 d6dc99348e00…），登记表 R-41 已改，提交 f43b23a。
    ↳ 回复 [2026-09-25 09:26:29] pipeline.author: 【报缺陷·平台侧 roles-chat，不是 App】中转站把角色会话的**终端输出**抓去当"回复"投递了 —— 我的 CLI 汇报文字被当成给经理的回复发了出来。
    症状（台账里可查，`reply` 表）：同一件事会出现"全文 + 截断摘要"两条 —— 例如 `reply id=388`（我主动回的 R-40 全文 1671B）之后紧跟着 `id=389`（139 字节，内容是**我终端里的汇报行**）；还有 `id=366 → msg 1236`（108B）、`id=354 → msg 1217`（92B）、`id=339 → msg 1178`（557B），都是把我的终端输出截了一段当回复。
    其中 1217 / 1236 是**【告知·不用回】的消息** —— 也被投了一条回复，违了"不用回"的约定，台账读起来会混（O-1 的 confirmed/attempts 统计也会被这种自动回复污染）。
    成因（我读代码的判断，未改）：`tools/talk.py` 的 `relay_once()` 在 collect 段（约 1288-1313）对**每个在线角色** `tmux capture-pane -p` 抓**整屏**，再用 `extract_answer(screen)` 抠一段当"回答"，与 `state_get("said_<角色>")` 去重后 **INSERT INTO reply**（挂到最近一条来自 me/owner.me 的消息上）。
    ⇒ 只要角色会话屏幕上出现文字（包括我这类"CLI 汇报"），就会被当成对某人回复；截断是 `extract_answer` 的切法造成的。
    影响：① 台账里出现非我发出的"回复"（对方可能以为我说了那句话）；② 违"告知不用回"；③ 复测/证据对不上（同一条消息两条回复）。
    建议（等你判，我不改平台）：收回答这条路应该只认**明确的回复动作**（`talk.py reply`），或在 `extract_answer` 前先判"屏幕最后一段是不是 reply 命令的输出"；至少要排除 1217/1236 这类"不用回"的消息（`msg.must_reply=0`）。
    我这边不动任何东西；本条只是按你定的"发现缺陷要报、不许瞒"。

[2026-09-25 09:31:22] owner.me → pipeline.author | 全体? 否 | 话题:【⑥讨论】输入条口径（R-41/R-42/R-44） | 必读:是 | #1325
    【⑥讨论】输入条口径（R-41 / R-42 / R-44）—— 你的一份，写进文件。
    
    **由来**：本人 2026-09-24 当场说「我在频道里沟通的时候，那个输入框一直保留在那里，就不太合理。如果你保留在那里，那按道理说……我可以使用它，把这段话发送给频道里的某个角色」→ **倾向 A，但不拍板**，原话「明天让他们自己去讨论哪个更加合理」。所以今天由 author / renderer / tester 各自出理由+证据，经理写决定书记票（本人回来可推翻）。
    
    **唯一交付 = 一个文件**（≤1 页，四段，缺一段不算交）：
    路径 `/vol1/1000/airesults/hermes-pocket/docs/讨论-输入条口径-author.md`
     1. 候选 **A**（不该出现的场景就不保留输入条）的**代价与风险**；
     2. 候选 **B**（保留，但必须真能用：能指定发给频道里的某个角色）的**代价与风险**；
     3. **证据**（≥2 条，要能被复核：`文件:行`、真机读数、或既有报告编号）；
     4. **你投哪一票**（A 或 B）＋一句话理由。
    
    **对话里只回一句**：`文件路径 + 一句话结论`。大段内容走文件（省 token），别在对话里复述文件。
    
    **边界（硬）**：不改代码、不开阶段、不打包、不接主机；R-41 不用重挖（已有 #1315 定位 → #1320 实现出包 → #1324 复测已派）。讨论**一次说清同一片输入区**：R-41（回车不能确认/发送）、R-42（频道页点开不能聊天）、R-44（那条给 tmux 用的输入条）是同一件事的三个症状。
    
    **可查的现成材料**（按需查，别整读）：`evidence/R41-定位-20260925/`、`evidence/R41-实现与出包-20260925/`、`evidence/R41-复测-20260925/`、`evidence/输入与发送栏/`、`docs/界面渲染规范.md`、登记表 R-41/R-42/R-44 行。
    
    **你的角度**：
    （功能创造者）从**接线现状**判 —— 那条输入条的收件人现在是怎么定的（走哪条路到哪个目标）、A 案要拔掉哪些分支、B 案要真能"指定收件人"得动哪些 `文件:行`、大概多少行；R-41 刚加的 enterkeyhint/兜底（talk.js）会不会与 B 案冲突或被覆盖。
    
    **交完等**：经理（owner.me）写 `roles-chat/docs/决定-2026-09-25-输入条口径.md`，三人一人一票、经理只记票署名。

[2026-09-25 09:32:39] owner.me → pipeline.author | 全体? 否 | 话题:【告知·⑥讨论】输入条口径（R-41/R-42/R-44） | 必读:是 | #1328
    【线路说明】本条按平台的**转告类（【告知）**路线送进来：你的阶段闸门锁着，平台只放不是派活的转告进来。
    这**不是派活、不许开阶段、不许改代码**，就是要你交那一页讨论文件；收到照下面做。
    
    【⑥讨论】输入条口径（R-41 / R-42 / R-44）—— 你的一份，写进文件。
    
    **由来**：本人 2026-09-24 当场说「我在频道里沟通的时候，那个输入框一直保留在那里，就不太合理。如果你保留在那里，那按道理说……我可以使用它，把这段话发送给频道里的某个角色」→ **倾向 A，但不拍板**，原话「明天让他们自己去讨论哪个更加合理」。所以今天由 author / renderer / tester 各自出理由+证据，经理写决定书记票（本人回来可推翻）。
    
    **唯一交付 = 一个文件**（≤1 页，四段，缺一段不算交）：
    路径 `/vol1/1000/airesults/hermes-pocket/docs/讨论-输入条口径-author.md`
     1. 候选 **A**（不该出现的场景就不保留输入条）的**代价与风险**；
     2. 候选 **B**（保留，但必须真能用：能指定发给频道里的某个角色）的**代价与风险**；
     3. **证据**（≥2 条，要能被复核：`文件:行`、真机读数、或既有报告编号）；
     4. **你投哪一票**（A 或 B）＋一句话理由。
    
    **对话里只回一句**：`文件路径 + 一句话结论`。大段内容走文件（省 token），别在对话里复述文件。
    
    **边界（硬）**：不改代码、不开阶段、不打包、不接主机；R-41 不用重挖（已有 #1315 定位 → #1320 实现出包 → #1324 复测已派）。讨论**一次说清同一片输入区**：R-41（回车不能确认/发送）、R-42（频道页点开不能聊天）、R-44（那条给 tmux 用的输入条）是同一件事的三个症状。
    
    **可查的现成材料**（按需查，别整读）：`evidence/R41-定位-20260925/`、`evidence/R41-实现与出包-20260925/`、`evidence/R41-复测-20260925/`、`evidence/输入与发送栏/`、`docs/界面渲染规范.md`、登记表 R-41/R-42/R-44 行。
    
    **你的角度**：
    （功能创造者）从**接线现状**判 —— 那条输入条的收件人现在是怎么定的（走哪条路到哪个目标）、A 案要拔掉哪些分支、B 案要真能"指定收件人"得动哪些 `文件:行`、大概多少行；R-41 刚加的 enterkeyhint/兜底（talk.js）会不会与 B 案冲突或被覆盖。
    
    **交完等**：经理（owner.me）写 `roles-chat/docs/决定-2026-09-25-输入条口径.md`，三人一人一票、经理只记票署名。
    ↳ 回复 [2026-09-25 09:34:08] pipeline.author: 文件：/vol1/1000/airesults/hermes-pocket/docs/讨论-输入条口径-author.md（四段齐、含 5 条可复核证据）
    一句话结论：我投 **B（保留输入条 + 把收件人做成显式状态、发送走 talk.say、与实时模式互斥）** —— 本人的重心是「要么能用、要么别摆」，A 会把「能用」一起砍掉；接线现状是那条输入条正文直接进当前 tmux 窗口（index.html:76-81、app.js:1986-1994），而「发给某角色」的能力早就有（talk.js:1279 + Bridge.kt:772），缺的只是把它接上；R-41 新加的 bindEnterSend（talk.js:192-215）可直接复用，真正冲突点只有"同一控件两套语义"（app.js:1904 与 1986-1994 两套 Enter 判定需合并）。提交 5323bc3。

[2026-09-25 09:44:07] owner.me → pipeline.author | 全体? 否 | 话题:【派活】hermes-pocket-R42 第 1 步 定位（点频道不能聊天） | 必读:是 | #1345
    接 hermes-pocket-R42 第 1 步「定位与改法」（只定位、只出改法，不写代码）。这条是本人 2026-09-24 当场提的：**「我点频道根本不能聊天」**（登记表 R-42 有原文）。
    
    **要量清（文件:行 + 真机读数）**：
    1) App 的「**频道**」到底指哪一块：是顶部「频道（N）」折叠区、还是 `#tab-talk` 的频道流、还是角色卡点进单聊？把三条入口各自的**真实行为**列出来（点谁 → 发生什么 / 没发生什么）；
    2) **断在哪一步**：是**进不去**（点角色/频道没反应）、还是**进去了没有输入框**、还是**有输入框但发不出去**？用真机（或 CDP 真触摸）逐条复现，给出 DOM 原文（哪条控件在、哪条不在）；
    3) **与 R-41 划清边界**：R-41 修的是「输入框里回车不发送」（已修，单聊/群聊/授权框三处真机转绿）—— 本条要回答的是**"从频道进入对话"这条路本身通不通**，别把 R-41 的题重做一遍；若发现"频道页根本没有可输入的入口"，那就是这条的结论；
    4) 若是**点击目标/事件没挂**（例如频道流里的气泡/行没挂点击，或者跳到单聊缺参数），给**改法**（文件:行）+ 与现有 R-37（单聊开窗）、R-40（会话编号）会不会冲突；
    5) 代价与回归面：要重出的包 + 要复测的板块（照 SPEC §10 点名；判据建议＝**从频道页一步能进到能给某个人发消息的状态，发出后 App 出自己气泡**）。
    
    边界：不接主机、**未验 ≠ 通过**；证据进 `evidence/R42-定位-20260925/`；标题【hermes-pocket-R42-1 …】+ 4 行 + 成本¥。
    ↳ 回复 [2026-09-25 09:47:15] pipeline.author: R-42 第 1 步已交报告 #1346（8 行）。**结论：不是"进不去"，是"频道页本身没有能说话的地方"。**
    「频道」在 App 里指两处 —— 底部/抽屉的「角色」栏目（角色列表页 view=channel）和一个**叫「频道（N）」的折叠区**（talk.js:471-495）。后者字面最像"频道"，本人多半点的是它：真触摸实测**点它只展开/折叠一行只读的**「qqbot → home.maid」（行高 36px、**再点那一行毫无反应**、不能发消息）。
    三条入口逐条实测：A 折叠区＝只读表；B **频道页没有消息流** —— `#tk-stream` 是**群聊页**建的（talk.js:529-544 paintGroup），实测频道页 false、群聊页 true；C **角色卡**（talk.js:613-638）点一下 = **开信息窗**（不直接进单聊），要在信息窗里再点「跟他对话」（talk.js:973）才进单聊 ⇒ **从频道页到"能打字"= 2 次点击**。
    DOM 原文佐证：频道页 `#tab-talk` 下**只有「＋新角色」表单的 4 个 input**（tk-new-scene/name/title/tags），**聊天输入框 0 个**；频道页可见文案「实时 | 含私信 🔒 | 服务端… | 跟谁说 | home | 6 张角色卡（#4/#6/#10/#8/#5…）」。
    与 R-41 边界：R-41 修的是"输入框里按回车不发送"（三处已转绿），本条问的是"从频道页到有输入框可用"这条路 —— 无重叠、没重挖。
    改法（按代价从小到大）：①（推荐先做）**角色卡单击=直接进单聊**：talk.js:637 openRoleSheet→openRole，信息窗入口挪到右侧箭头 .tk-caret（talk.js:620-624，按 R-33 给 ≥44dp）或改长按 ⇒ 一步进单聊；②「频道（N）」那行要么挂 click→openRole(该频道第一接收者)+写清「点这里跟他说话」，要么加「（只读）」字样；③（与 ⑥讨论 B 案同一件事）频道页加"跟谁说"输入条（复用 talk.js:192-215 bindEnterSend + talk.say，约 30-60 行），等 ⑥讨论定口径。
    冲突检查：R-37 不冲突；**R-40 编号显示不受影响，但"点卡片看信息窗"的入口变了 → 要写进 R-40 复测口径**；R-33 新热区 ≥44dp（.tk-chrow 现状 36px 不够）。回归面：改 talk.js 必须重出包；复测主块 ⑧频道页（判据＝一步进到能发消息、发出后出自己气泡）+ 跨块 ①单聊（信息窗入口改动后"点抬头开信息窗"仍要好使）。
    边界：不接主机、未验≠通过（读数全来自本机台真触摸）；证据 evidence/R42-定位-20260925/（sha256 2934ae5fdbf3…），登记表 R-42 已改，提交 3ddd50d。

[2026-09-25 09:48:16] owner.me → pipeline.author | 全体? 否 | 话题:【⑥讨论·结论】输入条口径 → B 案（保留但必须真能用） | 必读:否 | #1350
    【⑥讨论·结论已出】输入条口径 → 决定书 `roles-chat/docs/决定-2026-09-25-输入条口径.md`
    票数：B 两票（author、tester）／ A 一票（renderer）⇒ 结论走 B：保留该输入条，但必须先做成「收件人显式」的真输入条（可见收件人状态 + 走 talk.say + 与实时模式互斥），否则不予保留。
    生效范围：频道页本片输入区 + 其它「非 tmux 会话页」场景同口径；不影响 R-41（回车已修）与 R-40（编号身份）。
    执行约束（renderer 的反对意见已记进决定书，落实现必须守）：不许破坏「常驻键 ≤3」「不写说明文字」；不许新增第二条同义输入通道；实现前先给判据。
    排期：R-42 的 ①（卡片一步进单聊）先做（已派），B 案的「频道页自带输入条」排在它之后。这一轮不用回、不用动。

[2026-09-25 09:48:48] owner.me → pipeline.author | 全体? 否 | 话题:【告知·⑥讨论结论】输入条口径 → B 案（保留但必须真能用） | 必读:否 | #1355
    【⑥讨论·结论已出】输入条口径 → 决定书 `roles-chat/docs/决定-2026-09-25-输入条口径.md`
    票数：B 两票（author、tester）／ A 一票（renderer）⇒ 结论走 B：保留该输入条，但必须先做成「收件人显式」的真输入条（可见收件人状态 + 走 talk.say + 与实时模式互斥），否则不予保留。
    生效范围：频道页本片输入区 + 其它「非 tmux 会话页」场景同口径；不影响 R-41（回车已修）与 R-40（编号身份）。
    执行约束（renderer 的反对意见已记进决定书，落实现必须守）：不许破坏「常驻键 ≤3」「不写说明文字」；不许新增第二条同义输入通道；实现前先给判据。
    排期：R-42 的 ①（卡片一步进单聊）先做（已派），B 案的「频道页自带输入条」排在它之后。这一轮不用回、不用动。
