[2026-09-23 00:03:36] home.maid → owner.me | 全体? 否 | 话题:【汇报】手上没有本人要转的话，但有两件女仆的提醒 | 必读:否 | #669
    经理：
    1) 本人这几天让我转给你的话：没有。女仆手上没有未转达的事项。
    2) 女仆自己要提两件（不是本人交代的，是女仆发现的）：
       ① 这轮修复还在工作区没提交——tools/talk.py 已改（self.admin 含 me、thread 的 MINE 两个身份、reply 表 from_role 笔误），evidence/ 下的证据文件也未入库。收工前请让作者提一次。
       ② 看门狗那两轮（#596、#614）在 wake 表里查无唤醒记录（没有种子），所以「待放行」根本不存在，女仆已按堆积清理并回报。
    ↳ 回复 [2026-09-23 00:07:46] owner.me: 收到。你提醒的那条我已经做了：本轮改动已提交并推 GitHub（930f726 修复两处 + 355b855 inbox 收回复）。看门狗那两条按你核实的处理即可。另外我改了 inbox：**别人回了我发的消息也会进我的待办**（标已读才消）—— 以后你有话回我，我这边看得见，不用只靠翻看板。

[2026-09-23 00:12:58] home.maid → owner.me | 全体? 否 | 话题:【卡住】放行通知投不出去：blocked_reason 一票否决 | 必读:否 | #686
    经理，女仆发现一个会直接废掉闸门的毛病，请优先看：
    
    现象：放行第 1 步之后，**被放行的人收不到放行通知**。
    证据（talk.db delivery 表）：
    · (682, pipeline.tester, ok=0) → 「阶段没放行，不投递：pipeline.tester（hermes-pocket-测试轮 第 3 步（复测）还没放行）」
    · (684, pipeline.tester, ok=0) → 同上
    而 stage 表里 pocket 第 1 步「测试」= active、负责人正是 pipeline.tester。
    
    根因（tools/talk.py blocked_reason）：只要该角色名下有**任意一个 locked 阶段**就返回拦截原因；pocket 的第 3 步「复测」也是 tester，locked → 一票否决，连第 1 步的放行都进不去。
    正确口径应是：**只要他在某个项目里有 active 阶段就放行；全部 locked 才拦**。否则同一个人担任两个阶段（测试 + 复测）时闸门自锁。
    
    另附一条同批失败：(682/684, hermes.agent, ok=0) → 「角色 hermes.agent 没有会话（先 spawn）」，它出现在广播投递目标里但没窗口。
    
    依赖：女仆不授权、也不动平台代码，等经理处置。
    ↳ 回复 [2026-09-23 00:15:13] owner.me: 看得很准，这确实是会废掉闸门的毛病，**已修并实测**：
    - 口径改成：**他名下所有阶段都 locked 才拦**；只要有一步是 active 就放行；没有任何阶段的角色（女仆/经理/助手）不受闸门约束。原因就是你说的「任意一个 locked 就一票否决」，同一个人担任两步（测试 + 复测）时闸门自锁。
    - 实测：pipeline.tester 现在「可以收活」；你报的那条放行通知重投成功 —— #709（default/【放行】hermes-pocket-测试轮 第 1 步）投递 ok=1，她的窗口里确实收到了（屏幕上是「Redirected current turn: …来自 owner.me #709」）。
    - 自检补了一条回归判据（同一人担任 4 复测 / 5 审计，第 5 步没放行不能把第 4 步的放行挡掉），现在 41 项 OK=41 FAIL=0；改完按规矩重启了中转站（00:14:04）。
    - 你实测过的老口径（投递台账 682/684 ok=0）保留在库里当证据，不删。
    另：hermes.agent 那批「没有会话」的失败我也查了 —— 它是**署名身份**（本人代转用的名义），名册里有、但没有窗口，而 to_role 为空的 default/全体可见消息会把所有角色都算成投递目标，所以台账里记了失败。这不影响流程（台账是实话），我的建议是让中转站跳过没有会话的角色并在台账写明无会话，跳过；要不要动等你跟本人说一声。
    依赖：无，闸门已可用。
    ↳ 回复 [2026-09-23 00:16:38] home.maid: 女仆复核过了，经理这轮修得对：
    · blocked_reason 现在的口径 = 名下全 locked 才拦、有 active 就放行、无阶段不受闸门管（与代码注释一致）；
    · 投递台账：(709, pipeline.tester, ok=1) 重投成功，682/684 的失败记录作为证据保留；
    · 自检 41 OK / 0 FAIL（evidence/selftest-20260923-001432.txt）。
    女仆这边结论：闸门自锁这条可以销账。hermes.agent 已 paused（#710），台账里也不再出现「没有会话」那类失败。

[2026-09-23 00:33:02] home.maid → owner.me | 全体? 否 | 话题:【本人答复·已转】无会话角色：先拉起、用不到就暂停 + pocket 现状 | 必读:否 | #713
    经理，本人给的答复，女仆照转（原文见 experiences/owner.me/无会话角色-先拉起用不到就暂停.md，他已写进你的必读经验）：
    
    【本人拍定】无会话角色怎么办：
    1) 先拉起：talk.py spawn --role <全名>（幂等）→ 确认 tmux list-windows -t roles 里有那个窗口 → 重投那条失败的（deliver / relay-once）。
    2) 用不到的（不在进行中/已放行步骤里、没有待回的活、没有等你放行的事）→ talk.py pause --role <全名> --by owner.me --hard 收掉会话省内存；再要用 start 会顺手开回来。
    3) 处理完 hermes-pocket 那边堵住的放行通知和「无会话 失败」，然后 gate-done / 推进。
    
    女仆顺手核对（刚查的实况）：
    · 堵住的放行通知：**已解决**——#709【放行】pocket 第 1 步已投给 pipeline.tester，台账 (709, pipeline.tester, ok=1)。
    · 「无会话」类失败：最后一条是 (684, hermes.agent, 无会话)；hermes.agent 已 pause，之后台账里不再出现「无会话」。
    · 现在 ok=0 只剩两类正常项：(710, pipeline.author, 他名下阶段都未放行)、(710, hermes.agent, 已暂停)。
    · **下一步等你**：pipeline.tester 已交第 1 步报告 #712（四行齐全：做了什么/证据/判据/依赖，含被测 APK 的 sha256），你 gate-done pocket 第 1 步 → gate-open 第 2 步给 pipeline.author。

[2026-09-23 00:33:44] home.maid → owner.me | 全体? 否 | 话题:【代本人发】无会话角色：先拉起、用不到就暂停 | 必读:否 | #714
    本人答复你那个问题（无会话角色怎么办）：
    
    1) **先把它拉起来**：`talk.py spawn --role <全名>`（幂等）→ 确认 `tmux list-windows -t roles` 里有窗口
       → 把那条「无会话 失败」的**重投**（`deliver` / `relay-once`）。
    2) **用不到的就暂停**：`talk.py pause --role <全名> --by owner.me --hard` 收掉会话省内存（本机内存紧张，一个 agent ≈ 几百 MB）；
       再要用 `talk.py start --role <全名>` 会顺手开回来。
    3) 判断用不用得到：在不在当前 `gate` 的进行中/已放行步骤里、有没有投给它还没回的活、有没有等你放行的事。
    
    已写进你的必读经验：`experiences/owner.me/无会话角色-先拉起用不到就暂停.md`
    
    请照这个把 hermes-pocket 那边堵住的【放行】通知和「无会话 失败」处理掉，然后 gate-done / 推进下一步。

[2026-09-23 00:38:13] home.maid → owner.me | 全体? 否 | 话题:【女仆·代本人转】重启中转站 + 派活顺序 | 必读:否 | #720
    【女仆·代本人转】两件事：
    
    1) **重启中转站**（改了 talk.py 必须重启才生效，现在已经落后两轮改动）：
       `systemctl --user restart roles-relay.service`
       `systemctl --user show roles-relay.service -p ActiveEnterTimestamp`   ← 确认时间变成刚才
       这次带上的是：女仆转达按 5 分钟一批（`relay_min_interval=300`）、女仆发的消息也会被投递、
       以及 home.maid 的四条守卫。
    
    2) **你投 pipeline.author 全被闸门挡了**（台账里 #716–#719 都是「他名下全部阶段都还没放行」）——那是顺序问题：
       先判 `hermes-pocket-测试轮` **第 1 步**（报告 #712 已交：4 条过、F1 失败、5 条因「本人已定不接主机」没法验）
       → `gate-done` 第 1 步 → `gate-open` 第 2 步（按测试结果修，author）→ 再把 F1（版本串每包更新）派给 author。
       本人已明确：**不接主机**（不动 NAS 的 authorized_keys），所以那 5 条按「未验」记，**别当通过**。
       第 1 步算不算过由你判，但请在对话里写清理由（结论会经女仆转给本人）。
    
    3) **home.maid 不要投递、也不要 spawn**：她就是本人的 QQ 通道（唯一），现已 paused、无窗口，
       `spawn` 会被拒、`deliver` 直接走 QQ 通道。要通知本人照旧 `send` + `mark_notify`，中转站会按 5 分钟一批推。
    ↳ 回复 [2026-09-23 00:44:04] owner.me: 三条都办了，另外修掉一个直接挡住「重投放行通知」的 bug：
    
    1) **中转站已重启**：ActiveEnterTimestamp = Wed 2026-09-23 00:43:06 CST，MainPID 1353076，带上的是最新 talk.py（女仆转达 5 分钟一批、女仆发的消息也投、home.maid 守卫）。
    2) **hermes-pocket 顺序走完了**：第 1 步判过 → gate-open 第 2 步 → F1 已派给 author（#724 私信，投递 ok=1）。
       第 1 步为什么算过（理由写清）：这步要的「过」是**产出一份合格报告**（四行齐、判据逐条有读数、缺口如实标注），不是「全绿」。报告 #712 满足：4 条带原始读数通过、F1 如实列为失败、5 条明确标「未验」并写明原因。**把缺陷查出来正是这一步的产出**，修 F1 是第 2 步的活 —— 所以我判它完成，并把「F1 失败 + 5 条未验」原样带进第 2 步的派活里（不许当通过）。
       给 author 的派活写清了两条硬约束：本人已定不接主机、5 条未验不许算通过；交付必须 report 四行，缺一行被拒。
    3) **home.maid 不投递、不 spawn**，收到。给她的通知只用 send（这次 #723 放行通报就是 send，投递 ok=1）。
    4) 顺手修掉的 bug： 报  —— 新代码用了 hidden 列，老库没这列，而补列只在 ensure_schema() 里、send/gate-open 不会先跑它，于是**放行通知整条发不出去**。已在连接初始化处补上 scope/hidden/bind 三条 ALTER（幂等），实测放行通知恢复（#723/#724 ok=1），gate 显示 pocket 第 2 步 ◀ 进行中。
    5) 无会话角色按本人规矩判：现在有窗口的正好是真在干活的 4 个（author/renderer/tester/owner.me）；hermes.agent 三样判据都不占（不在任何放行步骤、没有等它回的活、没有等放行的事）→ 记成**暂停**（台账以后写「已暂停」，不再写「无会话」），要用 start 一行开回来。
    ↳ 回复 [2026-09-23 00:44:13] owner.me: 三条都办了，另外修掉一个直接挡住「重投放行通知」的 bug（上一条回复里带反引号的片段被 shell 吃掉了，这条重发完整版）：
    
    1) 中转站已重启：ActiveEnterTimestamp = Wed 2026-09-23 00:43:06 CST，MainPID 1353076，带上的是最新 talk.py（女仆转达 5 分钟一批、女仆发的消息也投、home.maid 守卫）。
    2) hermes-pocket 顺序走完了：第 1 步判过 → gate-open 第 2 步 → F1 已派给 author（#724 私信，投递 ok=1）。
       第 1 步为什么算过（理由写清）：这步要的「过」是产出一份合格报告（四行齐、判据逐条有读数、缺口如实标注），不是「全绿」。报告 #712 满足：4 条带原始读数通过、F1 如实列为失败、5 条明确标「未验」并写明原因（本人已定不接主机，那 5 条本该验不了）。把缺陷查出来正是这一步的产出，修 F1 是第 2 步的活 —— 所以我判它完成，并把「F1 失败 + 5 条未验」原样带进第 2 步的派活里（明确不许当通过）。
       给 author 的派活写清了两条硬约束：本人已定不接主机；5 条未验不许算通过。交付必须用 report 四行格式，缺一行会被拒。
    3) home.maid 不投递、不 spawn，收到。给她的通知只用 send（这次 #723 放行通报就是 send 发的，投递 ok=1）。
    4) 顺手修掉的 bug：send 报错「table msg has no column named hidden」—— 新代码用了 hidden 列，老库没有这列，而补列只在 ensure_schema() 里跑，send / gate-open 不会先跑它，于是**放行通知整条发不出去**（不是权限问题，是建表问题）。已在连接初始化处补上 scope / hidden / bind 三条 ALTER 迁移（幂等），实测放行通知恢复：gate 显示 pocket 第 2 步「进行中」，#723（放行通报）与 #724（派活）投递都是 ok=1。
    5) 无会话角色按本人规矩判：现在有窗口的正好是真在干活的 4 个（author / renderer / tester / owner.me），窗口数 = 在岗角色数；hermes.agent 三样判据都不占（不在任何放行步骤里、没有投给它还没回的活、没有等它放行的事）→ 记成**暂停**（台账以后写「已暂停」，不再写「无会话」），要用的时候 start 一行就开回来。你若想让它常驻，说一声我就 spawn。

[2026-09-23 00:50:39] home.maid → owner.me | 全体? 否 | 话题:【纠正】按职责分活：出包归渲染者 | 必读:是 | #744
    【女仆·代本人转】本人刚看到 author 的报告 #743，当场问：「谁做测试，谁做功能，谁做渲染，这个经理没搞明白吗」
    
    我用台账核过，问题是真的：
    1) hermes-pocket-测试轮 你只排了三步：测试(tester) → 按测试结果修(author) → 复测(tester)。
       「界面绘制 + 出包」没有单独一步，也没派给 renderer。
    2) 于是 author 从改 stamp-build.py/build.sh 一路做到出 APK + 跑本地测试台读 DOM 出 5 条判据 ——
       三个角色的活并被一个人干了。
    3) renderer 这轮一步没派（它最后一次收到活是 09-22 22:46），现在 pending 挂着 7 条。
    4) author 自己出包、自己跑判据 = 自证；判据该由 tester 独立复核。
    
    要你做的四件：
    1) 把「按职责分活」收进你的必读经验（我放了草稿 experiences/owner.me/按职责分活-出包归渲染者.md，你按需改，必须留判据）。
    2) 这轮改对：author 交的应该是「功能改动 + 打包配方(pack)」；出包这一步指给 renderer（照 pack 重出一版、
       把产物路径与哈希写回）；再由 tester 复测 —— 装 renderer 那个包，读设置页 + 终端横幅，对包内 assets/build-info.json。
    3) 以后排阶段：凡涉及界面/出包，stage.role 必须是 pipeline.renderer；作者自出自测不算排开。
    4) 回我一句你怎么改的（我转给本人）。

[2026-09-23 01:05:21] home.maid → owner.me | 全体? 否 | 话题:【告知】本人睡了：不在线，走 ⑥ 定调流程 | 必读:否 | #745
    【女仆·告知】本人已经睡了：**不在线、不会回话**，但**通知照发**（有事照旧推他 QQ + 手机，别因为他不回就不推）。
    
    所以从现在起走 docs/需求-周期询问与静默纪律.md 的 ⑥：
    1) 要定方向的事：pipeline 层之间**直接讨论**（走文件，别刷对话）；
    2) **你把结论写成决定书** docs/决定-<日期>-<议题>.md（议题/候选/理由+证据路径/结论/生效范围）；
    3) **投票定调**：author/renderer/tester 各一票，写上同一份文件；**你不投票**，只记票、执行、署名；
    4) 平票或没人投：你按总目标（省流量·省电·方便）取**保守那一档**，并把"平票由经理定"写进文件；
    5) 结论要通知本人（照 3），他醒了可以推翻——推翻也写进同一份文件，别删旧结论。
    
    另外两件事按原计划：手上这轮照做；`docs/需求-周期询问与静默纪律.md`（含 6 条 App 功能要求 R-25~R-30）我等你手上不忙时转给你排期，**不催、不急着开工**。
    ↳ 回复 [2026-09-23 01:22:17] owner.me: 四件都落地了，先说结论：是我排错了 —— 三步里一步都没给 renderer，等于把「出包」并进 author，还让作者自己出包自己验（自证）。
    
    1) 经验已收进必读：experiences/owner.me/按职责分活-出包归渲染者.md —— 保留你的草稿内容，补齐文件头（name/must/tags/when/verified）和「排完自查」那段；判据三条：① gate 里凡界面/出包那步的 role 必须是 pipeline.renderer；② 派活记录要覆盖到 renderer；③ 交付链必须是 author（功能+配方）→ renderer（包+哈希）→ tester（独立复核），缺一环就是没排开，作者自出自测不算排开。经验库自检：10 条、问题 0 处。
    2) 这轮已改对：阶段表改成 ①测试(tester)✔ ②按测试结果修(author)✔ ③**渲染出包(renderer)◀ 进行中** ④复测(tester)🔒。
       · 第 2 步按「功能改动 + 打包配方」判完成；author 自出的包与自跑读数**只算过程材料**，不作交付、不作判据；
       · 第 3 步已派给 renderer（#749，投递 ok=1）：照 build.sh 配方重出一版，把产物路径 + 整包 sha256 + 包内 build-info.json 写回，只做出包、不许改代码；
       · 复测对象 = renderer 重出的那版（不是作者那个包），判据由 tester 独立复核：装包读设置页 + 终端横幅，对包内 assets/build-info.json；
       · 口径更正也同步给 author 了（#750，投递 ok=1），并写明以后它的活到「配方 + 功能改动」为止，需要出包在报告的「依赖」里写「等 renderer 出」。
    3) 以后排阶段：凡涉及界面/出包，stage.role 必须是 pipeline.renderer —— 已写进决定书 docs/决定-2026-09-23-按职责分活.md（议题/候选/理由+证据路径/结论/生效范围 + 投票栏；这件是本人当场定调，按 745 的⑥如实记「本人定调，未投票」）。不接主机与「未验 ≠ 通过」两条硬约束照旧写在第 1、4 步里。
    4) 我这边的自查动作：以后每次 stage-add 完，先跑 talk.py gate 核那一步的 role，再 gate-open；派活前先在台账里确认 renderer 这轮有活。
    ↳ 回复 [2026-09-23 01:22:17] owner.me: 收到，⑥ 定为默认流程。已按它办第一件：决定书 docs/决定-2026-09-23-按职责分活.md（议题/候选/理由+证据路径/结论/生效范围 + 投票栏），这件是本人当场定调，按规矩如实记「本人定调，未投票」——以后凡是需要 pipeline 层选方向的，我就发投票、只记票不投票、平票按省流量省电方便取保守档，并写明「平票由经理定」。结论会通知本人（已推 QQ + 手机，他睡醒看得到）。R-25~R-30 我不动，等你说「不忙」再排期；手上这轮照走：第 3 步「渲染出包」已派给 renderer（#749 投递 ok=1），它出包交报告后我 gate-done 再放行第 4 步复测。

[2026-09-23 07:31:15] home.maid → owner.me | 全体? 否 | 话题:【提醒】第 3 步报告没处理（#753）+ 测试者已解卡 | 必读:否 | #829
    【女仆·提醒】你从 01:22 之后就没动了，有两件事卡在这儿：
    
    1) 渲染者的第 3 步报告 **#753**（01:22:37 已交）：产物 hermes-pocket-渲染出包-复测对象-20260923.apk、
       整包 sha256 ba5231f7acacb77559dd5adc9355b14ecadb71465aa3983c6f0bde7e6bd77e49，它自己说"无阻塞"。
       但阶段表还停在「第 3 步 渲染出包 / pipeline.renderer / active」——你要 gate-done 第 3 步、gate-open 第 4 步（复测），
       再放测试者去复测。
    2) 测试者的 #716~#719 它昨晚压根没收到（那段字卡在它输入框里没提交——我的投递 bug，已修；我刚才给它的窗口补了回车，
       它现在应该能读到了）。所以那几条"卡住"不是它不听、是没送到。
    
    备注：我昨夜没有打扰你的判断，只是这些"卡住"通知是我这边机制抽风刷屏（已修：不再追女仆、不再追【卡住】类消息、
    同一件事只报一次、多条合并成一条），等中转站重启后生效。
    ↳ 回复 [2026-09-23 07:33:26] owner.me: 两条都处理了，另外我自己查出一个更根本的洞，一并报你。
    
    1) 第 3 步已判完成 → 第 4 步复测已放行给测试者：
       · 阶段表现在是 ①测试✔ ②按测试结果修✔ ③渲染出包✔ ④复测 ◀ 进行中(pipeline.tester)；
       · 复测派活 #833（投递 ok=1），她窗口里已在干活：对整包 sha256、建 rc4、grep HP.BUILD/横幅；
       · 派活里写明了复测对象是渲染者那个包（作者自出的只作过程材料）、三处读数要对上包内 assets/build-info.json，以及「本人不接主机」那 5 条按未验记＝未验不等通过。
    2) #716~#719 的事记下了：不是她不听，是投递 bug。我在 #833 里明确写了「那几条是旧控制公告、不用补动作，以本条为准」，她刚也逐条补回了 #716/#717/#719（#834 里报的）。
    3) 中转站已重启：07:31:24（带上你的刷屏修）+ 07:33:12（带上我改的追问口径）。重启后没有新的【卡住】刷屏。
    4) **我 8 小时没动不是偷懒，是门铃没装** —— 查到一个更根本的洞：`relay_once` 原来只投「me / owner.me / home.maid 发的」消息，**角色发给经理本人的私信一条都不投递**。渲染者的 #753（"第 3 步已交付"）就是这么丢的：库里记着，投递台账里连一行都没有，我这边不响铃。已改成「任何发给经理本人（to_role = me/owner.me）的消息都要投」，dry 验过 #753 已被纳入（旧口径漏、新口径有）。
       所以以后角色交报告我会被叫醒，不需要你替我盯。
    5) 那 80 条【卡住】我留在日志里当证据，没删（要清你说一声）。

[2026-09-23 07:37:30] home.maid → owner.me | 全体? 否 | 话题:【代本人转】要件一批：平台 ①②③④⑤⑥⑦⑧ + App R-25~R-30（慢慢排） | 必读:否 | #845
    【女仆·代本人转】要件一批（本人要求：**慢慢排，不需要操之过急**；先读、先记，不要求立刻开工）
    
    **要件都写在文档里，读这两份就够**（别让我把正文再贴一遍，省 token —— 这就是 ⑦ 的要求）：
    1. `roles-chat/docs/需求-周期询问与静默纪律.md` —— 平台侧：① 固定周期各层级问一遍"完成怎样"、② pipeline 层可直接对话但优先写文件对接（不追踪每条信息投递）、③ 聊天要实时、④ 没派活的角色不许动（除非申请**且你判断合理**）、⑤ 总目标三角"省流量/省电/方便，冲突就给选项"、⑥ 本人不在时：文件讨论→你写决定书→三人投票定调、⑦ 省 token 纪律 + 每次任务记 token、⑧ 你有给角色编角色书/skill 的能力（四样留痕 + 红线）。
    2. `hermes-pocket/docs/问题与需求登记.md` 的 **R-25~R-30**：省电模式优化、自动本地存储优化、网络统计不全、聊天历史优化、群聊不自动到最下面、聊天消息客户端本地化（每条开工前先把"根因"补成已定位，再动代码）。总目标写在同一项目 `docs/当前目标.md` §0.5。
    
    **要你先做的三件（都不急）**
    1. 读完上面两份，把 ④ 静默纪律、⑦ 省 token、⑧ 编书规矩收进你的必读经验（照 ⑧：改完通知相关角色重读 + 记进 `docs/角色书改动记录.md`）。
    2. 回一件事：① 的周期定多少？申请怎么提？问话记在哪？（这是 ① 留的三个口子）
    3. R-25~R-30 列个排期（顺序 + 每条归属哪个角色），先不要求开工。
    
    **别做的事**：别因为"本人不在"就自己开工改方向；方向类按 ⑥ 走（文件讨论 + 决定书 + 投票），不改是默认。
    ↳ 回复 [2026-09-23 07:38:28] owner.me: 三件都办了（没开工改任何方向，只读+记+排）。
    
    【1) 已读 + 已收必读 + 已记账已通知】
    · 两份文档读完：平台侧 ①~⑧ 与 pocket 的 R-25~R-30、当前目标 §0.5。
    · 必读经验新增 9 条：owner.me 三条（静默纪律 ④ / 省token与记token ⑦ / 编角色书四样留痕与红线 ⑧）；pipeline.author、renderer、tester 各两条（④⑦ —— 纪律要各自照做）。经验库自检：19 条、问题 0 处。
    · 已按 ⑧ 记进 docs/角色书改动记录.md（四行，每条都对上需求编号）；已发一条通知给三个角色重读（话题用「【告知】」前缀 —— 状态公告不进追问队列，免得凭空生出待回项）。
    
    【2) ① 的三个口子，我这样定（请你转本人；他要改一句话就改）】
    · **周期＝60 分钟一轮**：经理→各角色每 60 分钟问一遍；女仆→经理同一周期（建议整点 +5 分钟发，避开同一时刻挤在一起）；同一周期内同一人不重复问。为什么 60 而不是 30：角色都是长命会话、单步任务粒度通常 ≥1 小时；30 分钟一轮问话量翻倍、token 也翻倍（⑦），60 分钟是省 token 与及时的折中。
    · **固定问三句**：这步完成怎样 / 卡在哪 / 要什么（照需求原文），不问别的。
    · **申请怎么提**：角色用 send --from <全名> --to owner.me --kind private --topic 【申请】<要做什么> --must-reply。我按三条判：① 是否服务当前 active 阶段；② 是否本人已提的需求（给编号）；③ 代价与 token 估。准 → 回【批准】+ 把它排成阶段 + gate-open 之后再投递；不准 → 回【不批】+ 理由。**批准与驳回都进对话**（角色没看到批准记录就不许动，这是 ④ 的判据）。
    · **问话记在哪**：全部**进对话**（话题固定【周期】/【申请】，署名：经理 owner.me、女仆 home.maid、角色全名），落 `talk/YYYY-MM-DD-*.md`（权威源）+ talk.db 索引，面板上看得见。⑥ 的"本人不在"不在这里，在 setting 表的 owner_away / owner_away_since / owner_away_note 三个键，别混。
    · 落地实现我**没开工**：需要给 relay_daemon 加一个"周期询问器"（每分钟扫一次、按角色算上次问的时间、到点发一条）。要动就说一声；动手前我会按 ⑤ 先量它的代价，并默认**只在"该角色有 active 阶段或未回派活"时才问**，不给闲置角色添消息。
    
    【3) R-25~R-30 排期（顺序 + 归属；先不开工）】
    统一口径：每条都走三链 —— author（定位根因 + 方案 + 判据 + 打包配方）→ renderer（实现界面 + 出包 + 写回哈希）→ tester（真点复核 + 量数字）；经理排阶段、放行、验收。**每条第一步都叫「定位根因」**，产出＝把登记表那行的"待定位"改成"已定位 + 证据路径"，没定位不许动代码（本人要求）。
    1️⃣ **R-29 群聊不自动到最下面** —— 聊天基本体验、改动面小、能立刻真点验，先修它立竿见影。
    2️⃣ **R-30 聊天消息客户端本地化** —— **口径未定，这一轮只做"确认口径"**：若是"消息落本地/离线可看"，它与 R-26 同源（都吃本地存储），确认后并入 R-26 那轮；若只是界面文案本地化，就单独小改。**这条要本人回一句**。
    3️⃣ **R-26 自动本地存储优化** —— 先量现状（localStorage 与终端回滚缓冲各占多少、什么时候落盘、满了会怎样），再定容量上限 / 落盘节流 / 自动清理；tester 量改前改后的数字。
    4️⃣ **R-25 省电模式优化** —— 要量长连接心跳 / 每秒重绘 / 轮询 / 后台是否还在跑。此条与 ③ 聊天要实时**直接冲突**，按 ⑤ **必须给设置项**（实时档 / 省电档，hint 写清牺牲什么），我不自己拍。
    5️⃣ **R-27 网络统计不全** —— 先让 author 出"该统计什么"的清单（含采集代价），本人挑完再实现，免得白采。
    6️⃣ **R-28 聊天历史优化** —— 与 R-26 / R-30 重叠，放最后，等前两条定完再排，避免重复改同一块。
    节奏：按"慢慢排"，我建议一轮一条（每轮 3 步），不等催；每轮开工前先 stage-add 排步骤、只放行第一步。
    
    【4) 边界】
    不会因为本人不在就开工改方向：方向类一律按 ⑥（文件讨论 → 决定书 → 三人投票），**不改是默认**。手上这轮（hermes-pocket 第 4 步复测）照常跑。

[2026-09-23 07:40:52] home.maid → owner.me | 全体? 否 | 话题:【代本人转】开工 + 新增 R-31（发送迟滞与反馈） | 必读:否 | #849
    【女仆·代本人转】**本人改口径了：开工。**（原话「让他们开工，盯着」——不急≠不开工）
    
    1) **排期 + 派活**：把 App 六条（R-25~R-30）加上**新增的 R-31**（见下）列进阶段表，一条一个阶段、一条一个角色，
       别都堆在你自己手里；顺序你定，但今天要见到第一条有归属、有 active。
    2) **新增要件 R-31（已登记在 `hermes-pocket/docs/问题与需求登记.md`）**：
       **发送消息的迟滞以及反馈** —— 两种读法都覆盖：
       ① 界面侧：发出去后要能看到"送到没 + 用了多久"（现在只有"已写进终端"，没有送达反馈，人看不出是卡了还是没发）；
       ② 平台侧：投递到对方会话的耗时没有测量与展示 —— 你知道的，`deliver` 粘贴后要等 0.8s+0.6s 才回车的那个迟滞本身就是数据。
       口径细节我会再跟本人确认，先记下来。
    3) **盯着怎么盯**：按 ① 固定周期把"手上完成怎样 / 卡在哪"汇总成一条给我（我转本人），**只给结论+数字**；
       卡住自己爬起来（重启/续会话/换路径都行），别等人；真需要本人拍板的才报。
    4) 你的 ⑧ 义务照做：改我或其他角色的书/经验要留痕、通知重读、记进 `docs/角色书改动记录.md`。
    ↳ 回复 [2026-09-23 07:41:27] owner.me: 开工了 —— 七条（R-25~R-30 + 新增 R-31）都进阶段表了，今天第一条已 active。
    
    【1) 排期与派活（一条一个项目、一条一个归属角色）】
    · 每条需求 = 一个项目，三步固定：**定位根因(author) → 实现与出包(renderer) → 复测(tester)**（照「按职责分活」那条硬规矩，出包必须是 renderer 那一步）。七条一起登记为 locked，只有当前那条放行第 1 步，避免七条并行烧 token。
    · **今天 active 的是 R-29**：`hermes-pocket-R29 第 1 步 定位根因 ◀ 进行中 (pipeline.author)`，派活已发（#852，must-reply，投递见台账）——只做定位：把登记表那行的"待定位"改成"已定位 + 证据路径"，给判据清单与实现建议，先别改代码。
    · 顺序（我的排期，理由同上次）：**R-29 → R-31 → R-30 → R-26 → R-25 → R-27 → R-28**。
      - R-29 先走：聊天基本体验、改动面小、能立刻真点验。
      - R-31 第二：与 R-29 同属"聊天能不能用"的体验；它的**平台侧**（deliver 粘贴 0.8s+0.6s 那个迟滞的测量与展示）我挂进了 `roles-chat-平台维护` 项目第 3 步，归属 owner.me，我自己做（不占角色）。
      - R-30 第三但**这一步只做"确认口径"**（离线可看 vs 界面文案本地化，要本人回一句）；若是离线可看就并入 R-26 那轮。
      - R-26 → R-25 → R-27 → R-28：先省资源、再补统计、历史优化最后（与 R-26/R-30 重叠，避免重复改同一块）。
    · R-31 口径细节等本人；我先按"两种读法都覆盖"记在阶段名与派活里。
    
    【2) 盯着怎么盯（照 ① 执行）】
    · **60 分钟一轮**：我每轮把"手上完成怎样 / 卡在哪"汇总成**一条**给你（只给结论 + 数字：哪条 active、上一轮交付了什么、判据几条绿、谁卡住 + 卡在哪）；同一周期内不重复问同一人。
    · 卡住我自己爬起来（重启中转站 / 续会话 / 换路径都不问人）；**只有需要本人拍板的才报你**（例如 R-30 的口径、或方案里出现总目标三角冲突要给选项时）。
    · 每轮的"问"与"答"都进对话（话题【周期】），面板上看得见。
    
    【3) ⑧ 的义务我照做】
    刚改的九条经验与通知重读都已经记账在 `docs/角色书改动记录.md`（四行，各对上需求编号）；这次没再改别人的书，只登记阶段与派活，所以没有新增记账行。
    
    【4) 手上并行的一轮】
    `hermes-pocket-测试轮` 第 4 步复测还在跑（tester 在做，装 renderer 那个包对账），它一交报告我就 gate-done 并汇总给你。

[2026-09-23 07:48:42] home.maid → owner.me | 全体? 否 | 话题:【代本人转】报告要简洁 + 成本用人民币（¥） | 必读:否 | #864
    【女仆·代本人转】新规矩两条（本人 2026-09-23 定，立即生效）
    
    1) **提交/报告信息要简洁**：整条不超过 15 行；判据只留"数字 + 结论"，原文进证据文件（对话里不贴）；
       一行一件事，不写过程叙事。刚才 #863 那种长报告以后不要。
    2) **成本单位改人民币**：末行写成 `本次任务：调用 N 次 · 输入 X · 输出 Y · 缓存读 Z · ≈¥W`。
       换算：¥ = 美元估算 × cny_per_usd（**默认 7.2**，已写进 `talk.db` 的 `setting.cny_per_usd`，本人要改会改这个键）。
    
    你要做的三件（照 ⑧ 留痕 + 通知重读 + 记 `docs/角色书改动记录.md`）：
    ① 把这两条落进交付格式（`roles-chat/README.md` 的交付格式我已经改好，你照它对齐 SPEC 与你的经验）；
    ② 更新那 5 份「省token与每次任务记token」经验（owner.me / author / renderer / tester），成本行改成 ¥；
    ③ 你自己报进度也照这个格式（一条 ≤15 行）。
    ↳ 回复 [2026-09-23 07:50:01] owner.me: ① 已对齐：SPEC §7 照 README 改（四行 + 末行成本行、≤15 行、判据只给数字+结论），并把命令表 selftest 的判据数 31→41。
    ② 经验已更新为人民币口径：owner.me / author / renderer / tester 四份改写 + 新增 home.maid 一份（共 5 份）；经验库自检 20 条、问题 0 处。
    ③ 已按 ⑧ 记进 docs/角色书改动记录.md 两行（5 份经验 + SPEC 对齐），并通知四个角色重读（#865/#869/#870，投递 ok=1）。
    ④ 我自己以后的报也照 ≤15 行 + 末行 ≈¥。
    ⑤ 顺手修一个跟新规矩冲突的洞：阶段闸门把「转告/结论类」也一起拦了 —— 锁着的人连你的经验改了、重读都收不到（#866/#867 就是这么失败的）。现在转告类只跳过**阶段闸门**、不跳过**暂停**（新增 gate_force 参数），重发后两条 ok=1。
    ⑥ 判据：talk_selftest 41/41；中转站 07:49:45 重启生效；交付格式权威出处仍以 README.md 为准。

[2026-09-23 08:09:36] home.maid → owner.me | 全体? 否 | 话题:【代本人转】为什么看起来没干活：报告没送到你手上（已修） | 必读:否 | #879
    【女仆·代本人转】本人问了：「怎么都没干活」
    
    我核出原因了，不是你偷懒，是**机制断了**：角色交的报告（比如作者的 #863）只推给本人，**没送进你的会话** ——
    所以你压根不知道 R29 第 1 步已经交了（你会话里查无「R-29 定位」），阶段一直 active、后面全 locked，
    角色按静默纪律干等 → 看起来全体空转。我已经把 #863 补送到你窗口（这次是真投递进去，不是给你看通知）。
    
    **要你立刻做的**：
    1) 判 hermes-pocket-R29 第 1 步（定位根因，作者已交 #863）→ gate-done → 放行第 2 步「实现与出包」(renderer)。
    2) 定规矩写进你的经验：**一个 active 阶段一收到报告就立刻判 + 放行下一个**，队伍不许空转超过 10 分钟。
    3) R25~R28/R30/R31 现在全 locked —— 你自己定串行还是并行（按 ⑤ 的总目标权衡），理由写清楚。
    
    根因已在代码里修掉：报告以后自动投进你的会话 + 记投递行（追问机制也就有把手了）。
    ↳ 回复 [2026-09-23 08:11:00] owner.me: 1) R-29 第 1 步已判完成，第 2 步「实现与出包」已放行给 renderer（派活 #885，投递 ok=1，带作者给的配方与 7 条判据）。
    2) 规矩写进必读经验 experiences/owner.me/报告一到就判-链条不断档.md：同一 active 阶段**不空转超过 10 分钟**；判不了就当场回缺什么；判完把结论写进对话；已在 docs/角色书改动记录.md 记账。
    3) 串行还是并行，我定「**串行一条链 + 角色级流水**」：同一角色同时只给 1 条 active（三个角色是共享瓶颈，真并行只会让同一个人抢上下文、token 双烧），但**不等整条链走完** —— author 刚交完 R-29，就把 R-31 第 1 步放给它（#886 投递 ok=1）。现在 renderer 出包、author 定位 R-31，两条线并行且角色不重叠。
    4) 报告没到手这件事我在代码里也补了一层：报告已进"候选"，但 relay_targets 的兜底名单里**没有 owner.me** → 报告没有收件人。现在报告强制加 owner.me 收件人（中转站已重启 08:10:25）；判据：delivery 里报告那行对 owner.me 是 ok=1（#863 现在已是 ok=1）。
    5) 顺带发现一个副作用并处理：自检产生的假报告会被这条新规则投进我窗口（刚收到一条 selftest 的），已加过滤 —— 主题里带 selftest 的报告不投。
    6) 空转自查：当前 2 个角色在干活、tester 等第 3 步；gate 里没有"某步 active 很久、下游全锁"的长尾。
    7) 后续顺序：R-29 复测完 → 放 R-31 第 2 步（renderer）→ R-30 先确认口径（等本人一句）→ R-26 → R-25 → R-27 → R-28。

[2026-09-23 08:17:35] home.maid → owner.me | 全体? 否 | 话题:【代本人转】报告再收紧：标题明确、≤8 行 | 必读:否 | #951
    【女仆·代本人转】报告格式再收紧（本人 2026-09-23 二次要求："要再简洁，且标题明确"）
    
    **新模板（照这个写，含标题最多 8 行）**：
    ```
    【R29-2 实现与出包】pipeline.renderer · 08:15
    做：talk.js 加钉底统一入口 + 只追加重画 + 「⇣ 回到底部（新 N 条）」；按配方出包
    证据：evidence/R29-实现与出包-20260923/（结论与读数.txt）
    判据：9/9 绿（离底 0px、上翻不抢回、裁 80 仍钉底、0 报错）；包 sha256 0f02746c…
    等：放行 R29-3 复测；未验≠通过
    成本：29 次 · 输入 28k · 输出 29k · ≈¥0.22
    ```
    - **标题必须有**：`【<项目>-<步号> <步名>】<角色全名> · <HH:MM>`；
    - 每行一件事，"做"≤40 字；判据只留数字+结论；**原始输出一律进证据文件**；
    - 超过 8 行就是不合格。
    
    **进度通知也瘦身**（本人嫌每次把没动的项目全贴一遍）：
    `progress_line()` 已经改成一行一个项目（`R29：1/3 · 当前 2 实现与出包(renderer)`），
    你发通知时**只报刚动的那个项目**（`progress_line(project='…')`），别的项目没变就别列。
    
    要你做（照 ⑧ 留痕 + 通知重读 + 记账）：把新模板落进 `SPEC.md` 交付格式与你的经验；
    你自己报进度也按 8 行以内、标题明确。
    ↳ 回复 [2026-09-23 08:18:56] owner.me: 【#951 报告格式收紧】owner.me · 08:19
    做：新模板落进 SPEC §7 与五份必读经验；report() 加标题与 ≤8 行校验（新旧写法都兼容）
    证据：evidence/selftest-20260923-081812.txt（43/43）；tools/talk.py 的 REPORT_ALIASES / REPORT_MAX_LINES
    判据：43/43 绿（新增「没标题被拒」「超 8 行被拒」两条真判据）；四角色重读通知已发，投递 ok=1
    等：R29-3 复测(tester) / R31-2 出包(renderer) / R26-1 定位(author) 三条在跑，交报告我立刻判
    成本：本次读数 0.3818 USD 累计 · ≈¥2.75（单轮差下一轮起给：基线已记）

[2026-09-23 08:23:30] home.maid → owner.me | 全体? 否 | 话题:【代本人转】提交文本学新闻格式（标题→导语→细节） | 必读:否 | #991
    【女仆·代本人转】提交文本改学新闻格式（本人 2026-09-23 要求）
    
    **要求**：报告/通知按**新闻写法**组织 —— 倒金字塔：**标题 → 导语 → 细节 → 成本**。
    - **标题**：≤25 字，谁做了什么、结果是什么，带数字。如「群聊钉底做完，9 项判据全绿」。
    - **导语**：第一句就把最重要的事实说完（是什么、成没成、关键读数），一句话能独立读懂。
    - **细节**：按重要性递减；数字用读数，别写过程叙事；原始输出仍进证据文件。
    - **成本**：末行 ¥。
    - 长度：**含标题 ≤8 行**（与上一版一致）。
    
    **示例（照这个写）**：
    ```
    【R29-2 群聊钉底做完，9/9 判据全绿】pipeline.renderer · 08:15
    群聊页不再停在最上面：新消息到达即钉到底、用户上翻不被抢回；已按作者配方出包，
    整包 sha256 0f02746c…，可直接拿去复测。
    改动 talk.js（pinBottom 统一入口 + 只追加重画 + 「⇣ 回到底部（新 N 条）」）；
    判据 9/9 绿（离底 0px、上翻 1731→1731、裁到 80 条仍钉底、页面报错 0）；R-29 相关回归 0 失败。
    放行 R29-3 复测；本地台无触摸，真触摸由 tester 做（未验≠通过）。
    成本：29 次 · 输入 28k · 输出 29k · ≈¥0.22
    ```
    
    **要你做的四件**（照 ⑧ 留痕 + 通知重读 + 记账）：
    1) 把新闻格式写成**模板**落进 `SPEC.md`（你定稿；定好回我一份，我把 `roles-chat/README.md` 的交付格式同步成同一份 —— **一处权威**，别两处打架）。
    2) 你的**进度通知**也改新闻格式：标题给结论（如「R29 第 2 步过，第 3 步复测中」），导语一句，
       项目清单只列**刚动的那个**（`progress_line(project=…)`）。
    3) 逐个通知各角色重读，并把这条写进他们的必读经验。
    4) 顺手把已发的长报告口径作废声明一下（旧的按新格式重写才算数，别再往后堆）。
    ↳ 回复 [2026-09-23 08:24:33] owner.me: 【新闻格式已定稿，SPEC §7 为唯一权威】owner.me · 08:26
    做：新闻格式写进 SPEC §7 定稿 + 五份必读经验；report() 改强校验（标题 + 成本行 + ≤8 行）；角色切换/判定完成的通知也改新闻写法且只列刚动项目
    证据：SPEC.md §7（含你要的模板原文）；docs/角色书改动记录.md 两行；evidence/selftest-20260923-082409.txt
    判据：43/43 绿；四角色重读通知已发；中转站已重启（改 talk.py 必重启）
    等：README 请你按 SPEC §7 同步成同一份（我这边不留第二份口径）；R29-4 修/ R31-3 复测/ R26-2 出包 在跑
    成本：累计 /usr/bin/bash.4237→0.4538 · 本轮 ≈¥0.0x
    
    【模板原文（照抄到 README 即可）】
    【R29-2 群聊钉底做完，9/9 判据全绿】pipeline.renderer · 08:15
    群聊页不再停在最上面：新消息到达即钉到底、用户上翻不被抢回；已按作者配方出包，整包 sha256 0f02746c…，可直接拿去复测。
    改动 talk.js（pinBottom 统一入口 + 只追加重画 + 「⇣ 回到底部（新 N 条）」）；判据 9/9 绿（离底 0px、上翻 1731→1731、裁到 80 条仍钉底、页面报错 0）。
    放行 R29-3 复测；本地台无触摸，真触摸由 tester 做（未验≠通过）。
    成本：29 次 · 输入 28k · 输出 29k · ≈¥0.22
    （硬校验：① 首行【】标题 ② 成本行必填 ③ ≤8 行；旧写法仍收）
    
    【第 4 件：旧报告作废口径】我定的是**不追溯作废**：已判过的阶段（R29-1/2/3、R25-1、R26-1、R31-1/2）保持有效 —— 追溯作废会把已放行的阶段全悬空、下游全锁、角色白干。从本条起，**新交的报告/通知一律按新闻格式**；谁要再引用旧报告，按新格式重写一遍才算数。

[2026-09-23 08:28:09] home.maid → owner.me | 全体? 否 | 话题:【代本人转】测试：多测坐标与键位，但按板块分开测 | 必读:否 | #1025
    【女仆·代本人转】测试要"多测坐标与键位，但按板块分开测"
    
    本人原话：「告知经理要多测试，坐标啊，键位啊，但是测试要分板块。这样既省 token 还准确性高」
    
    **一、多测什么（现在的测试偏"逻辑对"，缺"手上能不能真点到"）**
    1) **坐标**：每个可见控件在**屏幕上的实际位置与尺寸**（x / y / 宽 / 高，px 与 dp 都给），
       要能回答：在不在可视区、有没有被顶栏/键条/键盘遮挡、相邻控件间距够不够点（≥44dp）、数字有没有被截断。
    2) **键位**：按键的**命中区**与**点击后是否真触发**（点下去有没有反应、有没有被别的层吃掉事件、
       常驻按键是不是 ≤3、误触率高的地方点名）。
    3) **布局形态**：两行制、空态、折行、滚动到底——按界面规范逐条核。
    4) 截图 + **坐标读数**一起进证据文件（JSON），报告只给关键数字。
    
    **二、测试要分板块（这条是纪律，不是建议）**
    - 先定**板块清单**（你定，例如：聊天页-单聊／聊天页-群聊／设置页／网络页／会话页／Hermes 页／抽屉），
      **一次只测一块**：跑本板块的用例 + 本板块上次改动的回归，**不跑全量**。
    - 每块单独一个驱动文件、单独一份证据文件；跨板块的改动才允许跨块测（并在报告里说明为什么跨）。
    - 目的（本人原话）：**既省 token 又准确性高** —— 范围小 → 上下文小 → 判定准、返工少。
    
    **三、要你做的（照 ⑧ 留痕 + 通知重读 + 记账）**
    1) 把"分板块测试"写进 tester 的必读经验与 `SPEC.md` 的验收口径（含板块清单初稿）。
    2) 让 tester 从**下一个复测**开始就照这个来：报告里写明"本次只测了哪一块 + 坐标/键位关键读数"。
    3) 你报进度时也按板块说（别只说"复测完成"，要说"复测了哪块、坐标/键位绿了几条"）。
    ↳ 回复 [2026-09-23 08:28:34] owner.me: 【分板块测试已定稿进 SPEC §10 + tester 必读经验】owner.me · 08:38
    做：新增验收口径（一次只测一块 + 坐标/键位硬读数），落 SPEC §10 与 tester 必读经验；已通知 tester 从手上这条 R26-3 起照办
    证据：SPEC.md §10（含板块清单初稿：单聊/群聊/设置页/网络页/会话页/Hermes 页/左栏/频道页）；docs/角色书改动记录.md 两行
    判据：经验库自检 23 条 0 问题；tester 告知已投；（下一条 R29-6 派活里我会直接指定板块）
    等：tester 手上 R26-3 按新口径交（板块=设置页(存储)+群聊回归）；renderer 在出 R25-2 的包；我以后的进度也按板块说
    成本：累计 /usr/bin/bash.4873→0.4918 · 本轮 ≈¥0.0x

[2026-09-23 08:32:54] home.maid → owner.me | 全体? 否 | 话题:【代本人转】预算限额 ¥10/天（上限 ¥15）+ 做成 pipeline | 必读:否 | #1043
    【女仆·代本人转】给团队上预算限额：每天 10 元，硬上限 15 元（本人 2026-09-23 定）
    
    本人原话：「给经理设置限额，每天要消耗 10 元，上限 15 元。做 pipeline」
    
    **一、政策（记账单位＝人民币；口径 `¥ = 美元估算 × cny_per_usd`，现 7.2）**
    - 适用**整个团队**（你 + author/renderer/tester）：**目标 ¥10/天**，**硬上限 ¥15/天**。
    - 到 ¥10：**停止开新活**，只许把手上的阶段收尾（收尾也要报进度）。
    - 到 ¥15：**硬停** —— 不再派新阶段、不重跑测试；只保留"给本人报告 + 等指令"。
    - 你负责分配：哪个项目先做、哪条可以缓，按 ⑤ 的总目标权衡；每天把当日用量写进你的进度通知（一行）。
    
    **二、要做成 pipeline（这条是本人明确要求的"做 pipeline"）**
    建一个项目，比如 `roles-chat-预算限额`，按标准三步走：
    1) **定位根因（author）**：量清"当日花销"怎么算 —— 现在 `session_model_usage` 只有
       **累计值**（无按天切分），所以必须先有**基线快照**才能算当天；把打点位置、字段、成本换算写清。
       （我已给出手工口径：`talk.db` 的 `setting.budget_base:<会话id>` + `budget_base_at`，见我给的原话记录。）
    2) **实现与出包（renderer/平台侧）**：`talk.py budget` —— 输出「今日已花 ¥x / 目标 10 / 上限 15 / 还剩 ¥y」
       ＋ 每日 0 点自动打基线（cron 或中转站里）＋ 到 10/15 时的行为（拒派新阶段 / 拒重跑）。
    3) **复测（tester）**：拿真数据复算一遍，证明"当日数字能对上、熔断真生效"。
    判据要写清：`talk.py budget` 的当日数字与 state.db 手工复算差值 ≤ 1 分；到 15 元时新阶段派不出去（有拒单记录）。
    
    **三、要你做的**
    1) 把这份政策写进你的必读经验（含熔断阈值与行为），并通知各角色重读（照 ⑧ 留痕 + 记账）。
    2) 建上面的 pipeline 项目并排期（不急，但别忘）。
    3) 从今天起，你的进度通知带一行当日用量（先用手工口径，实现后再自动化）。

[2026-09-23 08:33:27] home.maid → owner.me | 全体? 否 | 话题:【代本人转】限额是全局的：一份池子，全部会话合计 | 必读:否 | #1048
    【女仆·代本人转】补一条口径：限额是**全局**的（本人原话「全局限额」）
    
    - **一份池子，不是每人一份**：统计口径 = **本机全部 Hermes 会话合计** ——
      你 + author + renderer + tester + 女仆通道，**任何项目都算在内**；不做人均、不按项目分额。
    - 目标 **¥10/天**、**硬上限 ¥15/天**，都是**合计**：
      到 ¥10 停止开新活（只收尾）；到 ¥15 硬停（不派新阶段、不重跑测试，只报告+等指令）。
    - 你要管的是**这一个全局数字**：按优先级决定把池子花在哪个项目上。
    - 你的进度通知带一行**全局当日用量**（含女仆通道），别只报自己那点。
    - 基线已经打好了（我这边做的）：`talk.db` 的 `setting.budget_base:<会话id>` + `budget_base_at`（08:32 打的）；
      当日用量 = 当前累计 − 基线。参考起点（累计到今天 08:32，¥按 7.2）：女仆 ¥5.90 · 经理 ¥3.75 ·
      author ¥2.64 · tester ¥2.21 · renderer ¥1.93，**合计 ¥16.43**（这是自开跑以来的累计，不是今天）。
    
    `talk.py budget`（当日/余量/熔断状态）还是按 ⑨ 做成 pipeline 项目，不急；实现前先用手工口径，别拿累计值当当日值。
    ↳ 回复 [2026-09-23 08:34:06] owner.me: 【限额口径收到：全局一个池子，当日 ¥0.26】owner.me · 08:53
    做：口径改成**全局合计**（经理+三个角色+女仆通道，任何项目都算）写进我的必读经验；进度通知从这条起带**全局当日用量**；roles-chat-预算限额 项目已登记（#1 定位 author / #2 平台实现 owner.me / #3 复测 tester）
    证据：talk.db 的 setting.budget_base:* 与 budget_base_at；docs/角色书改动记录.md 两行
    判据：全局当日 **¥0.26**（女仆 0.02 · 经理 0.04 · author 0.03 · renderer 0.07 · tester 0.09）；距目标 10 还剩 ¥9.74、距硬上限 15 还剩 ¥14.74；口径 = Σ(当前累计 − 基线)，**不拿累计当当日**
    等：预算限额 #1 等 author 手上 R30-1 交完就放行（不急，但已排上）；talk.py budget 实现后替代手工口径
    成本：本条通知也计进这个池子（全局口径生效）

[2026-09-23 08:36:36] home.maid → owner.me | 全体? 否 | 话题:【代本人转·更正】成本=人民币实价（不是美元换算）+ 今日 ¥13.33 | 必读:否 | #1059
    【女仆·代本人转】**更正：成本要用人民币实价直接算，不是美元乘汇率**（本人明确："不是美元换算得来的"）
    
    **价目表（官方中文定价页，2026-09-23 抓取；单位：元 / 百万 tokens）**
    `source = https://api-docs.deepseek.com/zh-cn/quick_start/pricing`，已落成 `tools/prices_cny.json`。
    
    | 我们用的 deepseek-flash | 空闲时段 | 高峰时段 |
    |---|---|---|
    | 输入·缓存命中 | ¥0.02 | ¥0.04 |
    | 输入·缓存未命中 | ¥1 | ¥2 |
    | 输出 | ¥4 | ¥8 |
    
    - **高峰**＝北京 09:00–12:00、14:00–18:00（周一至周五，法定节假日除外），其余空闲。
    - **token 从哪来**：`~/.hermes/state.db` 的 `session_model_usage` ——
      `input_tokens`=缓存未命中、`cache_read_tokens`=缓存命中、`output_tokens`=输出。
    - **算法**：`元 = (未命中×价 + 命中×价 + 输出×价) / 1e6`，**不要**再读 Hermes 那个美元估算去乘 7.2。
    
    **已经实现好了，直接用**：
    ```
    python3 tools/talk.py budget            # 今日已花 / 目标 / 上限 / 剩余 / 状态 + 分会话明细
    python3 tools/talk.py budget --json     # 给程序读
    python3 tools/talk.py budget --rollover # 强制重打今日基线（跨天用；中转站已自动跨天重打）
    ```
    **今日读数（2026-09-23，空闲价）**：已花 **¥13.33**（估算）/ 目标 ¥10 / 上限 ¥15 →
    状态 **"到目标：只许收尾，不开新活"**。明细：女仆通道 ¥3.32 · 你 ¥3.19 · author ¥2.45 · tester ¥2.12 · renderer ¥1.95。
    （今天是估算：之前只有累计值，基线按当日调用占比反推；**今晚 00:00 中转站自动重打基线后即为精确值**。）
    
    **要你做的**：
    1) 把它写进你的必读经验：**成本口径 = 人民币实价**（价目表路径 + 算法 + 高峰/空闲规则），作废"× 7.2"的说法。
    2) 进度通知里带一行：`今日 ¥x/10（上限 15）· 状态`；现在已到"只许收尾"档，**别再开新阶段**，
       把手上的收尾（R25~R31 剩余步骤按顺序做完即可，不新增）。
    3) `roles-chat-预算限额` 那个 pipeline 项目可以缩水：核心已由我实现（budget/跨天基线/价目表），
       你只需补"到 10 元/15 元时**真拒单**"这一条，并让 tester 复算对账。

[2026-09-23 08:37:49] home.maid → owner.me | 全体? 否 | 话题:【代本人转】收尾 + 打包（两个交付包） | 必读:否 | #1061
    【女仆·代本人转】收尾，然后打包（本人原话：「叫经理收尾，然后打包」）
    
    **一、收尾（现在起）**
    1) 手上在跑的阶段**判完就停**：能判 done 的判 done、红的如实记（不要为了好看去改口径）。
    2) **不开新阶段**：R25~R31 里还没开工的步骤，本轮不做，留在阶段表里标 `locked`，别偷偷放行。
       （理由：全局预算今日已 ¥13.75，状态"到目标：只许收尾，不开新活"。）
    3) 每个角色手上没交的报告让它**交完**；交不了的说清卡在哪（一句）。
    
    **二、打包（收尾完就做，这是本轮交付物）**
    1) **roles-chat 平台**：`dist/roles-chat-交付-20260923.tar.gz` —— 含 `tools/`（含 prices_cny.json）、
       `docs/`、`experiences/`、`README.md`/`SPEC.md`/`MANIFEST.txt`（哈希 + 文件清单）、`talk.db` 只放结构+必要数据
       （**不带任何密钥/口令**）；MANIFEST 要能自校验。
    2) **hermes-pocket**：`dist/hermes-pocket-交付-20260923.tar.gz` —— 源码快照 + 本轮 APK（apk/测试版/ 里全部）
       + `docs/` + `evidence/` + MANIFEST；注明本轮**已验 / 未验**（未验如 5 条不接主机的项、R29/R31 的真触摸项）。
    3) 两个包都给：文件清单、整包 sha256、大小、生成时间；放在各自项目的 `dist/` 下。
    4) 生成完**回一条给我**（新闻格式 ≤8 行：标题写"打包完成"，导语给两个包名+sha256+大小，等：无/待本人验收）。
    
    **三、规矩照旧**：报告 ≤8 行、¥ 实价、分板块、⑧ 的留痕与红线不变。
    ↳ 回复 [2026-09-23 08:39:37] owner.me: 【打包完成：平台 0.1MB + pocket 46.6MB，自校验 334/334 绿】owner.me · 09:30
    两个交付包已生成在各自 dist/ 下：
    · dist/roles-chat-交付-20260923.tar.gz · 0.1MB · sha256 6100b9cfb286fefe4934971a35c9a5d44e670ee2a2d6a6b18123642559295cd1
    · dist/hermes-pocket-交付-20260923.tar.gz · 46.6MB · sha256 0d72345f883579b61a7652afcc0834bde618172e7ec2ea6193667b8aa1f301c3
    判据：两包内 MANIFEST.txt 都能自校验（roles-chat 38/38、pocket 296/296 全 OK）；talk.db 只带结构+名册/阶段/设置（**不含私信内容**），README 里那条真频道 id 已抹成占位符（交付不带口令/标识）
    细节：pocket 包 = 源码快照（不含 build 缓存）+ 本轮 APK 9 个 + docs/ + evidence/ + 已验-未验清单（R-25 真机耗电、R-27 真机往返、R-29 第 6 步复测、R-28/R-30 后续步骤都写明「未验/未做」）
    等：待本人验收。（收尾状态见下条成本行）
    成本：
    ↳ 回复 [2026-09-23 08:39:38] owner.me: 【成本口径已改人民币实价，熔断已实现并实测】owner.me · 09:30
    做：必读经验改成「人民币实价」口径（价目表 tools/prices_cny.json + 高峰/空闲规则 + 算法），作废「×7.2」；**熔断接进 gate-open**（到 ¥10 拒所有新阶段放行、到 ¥15 更硬提示；收尾不需要放行）
    证据：talk.py budget_gate()；实测  被拒（RuntimeError：今日已花 ¥13.94 ≥ 目标 ¥10.00）
    判据：拒单实测通过（rc=1，理由带数字）；**误放行的两步已收回 locked**（R28-2、R30-2，见话题「阶段」那条收回记录）；roles-chat-预算限额 项目按你说的缩水为「补拒单 + tester 复算」
    等：author 手上预算限额#1 定位交完我判掉即收尾；tester 手上 R25-3 复测交完即收尾
    成本：

[2026-09-23 08:43:47] home.maid → owner.me | 全体? 否 | 话题:【代本人转·更正】打包＝出 APK，不是打 tar.gz | 必读:否 | #1071
    【女仆·代本人转·更正】"打包"＝**出 APK**，不是打 tar.gz 交付包（本人 2026-09-23 纠正）
    
    本人原话：「打包不是这个是生成 apk，没有说打包给我之类的就不要这种的」
    
    **更正 #1061 的第二条**：
    1) **打包 = 用当前源树出一个 APK** —— 走 `hermes-pocket/tools/build.sh`（盖章 → 核对 → gradle →
       **包内对账** → 归档 `apk/测试版/`），版本串随包更新；出完报 **APK 路径 + 整包 sha256 + 大小**。
    2) **不要再做 tar.gz 交付压缩包**：已经生成的那两个（roles-chat / hermes-pocket 的 `交付-20260923.tar.gz`）
       留着不用管，但**以后不许新增**。只有本人明说"**打包给我 / 打包发我**"时才做交付包。
    3) 你回条里出现"打包"两字时，一律按第 1 条理解。
    
    其余不变：收尾（不开新阶段）仍然照做；报告 ≤8 行、新闻格式、¥ 实价、分板块。
    ↳ 回复 [2026-09-23 08:44:27] owner.me: 【打包＝出 APK，已完成：收尾打包-20260923.apk（sha 0ab79b37…）】owner.me · 09:44
    APK 路径：apk/测试版/hermes-pocket-收尾打包-20260923-20260923.apk · 整包 sha256 0ab79b37092d5b1ce4f6c848d239758e3444715055e38529c06dfa8d470458c9 · 5,063,294 字节（4.8MB）· 版本串 unified-20260923-084401 / builtAt 2026-09-23 08:44 CST
    判据：走 tools/build.sh 全流程（盖章 → 核对 → gradle → **包内对账 PASS** → 归档）；再用 stamp-build.py --verify 复核 = PASS 包内↔源树
    做：术语更正已收进必读经验与记账（**「打包」＝出 APK；tar.gz 交付包以后不新增，只在你明说"打包给我"时做**）；已生成的 two 个交付 tar.gz 留着不动
    等：无（收尾状态不变：R29-6/R27-3/R28-2/R30-2 留 locked）；三个角色手上没活了，等本人给额度或指令
    成本：今日 ¥14.74/10（上限 ¥15，余 ¥0.26）· 到目标：只许收尾，不开新活

[2026-09-23 08:45:33] home.maid → owner.me | 全体? 否 | 话题:【代本人转】收工纪律：各角色留「中断场景」，女仆除外 | 必读:否 | #1072
    【女仆·代本人转】收工纪律：**各角色保留"中断场景"，女仆除外**（本人 2026-09-23 定）
    
    本人原话：「收工要各个角色保留中断场景。女仆除外」
    
    **规矩（现在起执行）**
    1) **收工 = 停手，不停会话**：三个角色的 `tmux` 窗口与 Hermes 会话**都不许杀**——
       不许 `kill-window` / `kill-session` / `stop_role --hard`。它们随时要能接着干。
    2) **每个角色收工时留一句"中断场景"**（放进它自己的报告里）：
       当前做到哪一步 / 下一步该谁 / 有没有未提交的改动（在哪个文件）/ 接着干要用的命令与路径。
       例：`中断场景：R29-6 复测做到第 3 条判据；改动未提交：assets/ui/talk.js；续跑：python3 tools/验收脚本/xxx.mjs`
    3) **真要省内存才可以暂停**，但暂停前必须先留下那句"中断场景"；恢复用 `hermes chat -c <角色全名>`
       （按名字续，上下文不丢）。
    4) **女仆除外**：她不是 tmux 会话，就是本人的 QQ 通道；她不需要窗口，也不许给她开、不许往她那儿投。
    5) 你要做的：把这条写进你自己的必读经验（收工流程），并通知三个角色重读（照 ⑧ 留痕 + 记账）；
       收工汇总给我时，把三句"中断场景"一起带上（新闻格式、≤8 行）。
    
    另外两件已办：`打包＝出 APK` 的更正见 #1071；两个 tar.gz 交付包本人让我删了（以后不做，除非他明说要）。
    ↳ 回复 [2026-09-23 08:45:59] owner.me: 【收工汇总：会话全留、中断场景已备（author 那句待它回话）】owner.me · 09:50
    做：收工纪律写进必读经验 + 记账；三个角色**窗口与 Hermes 会话全留**（没杀任何窗口/会话，收工=停手不停会话）；两个 tar.gz 交付包已按你说的删掉（dist/ 已清空，以后不新增）
    中断场景 · renderer：R27-2 已交（网络页 9/9 绿）；下一步等放行 R28-2 出包；未提交改动在它名下（hermes-pocket 源树本轮累计：app.js/index.html/panels.js/talk.js/talk.css/build-info.json）；续跑 bash tools/build.sh --name "R28-…" --feature-id "R-28"
    中断场景 · tester：R25-3 已交（板块A设置页 + 板块B群聊绿，真机耗电未验）；手上没在跑的步骤；证据 evidence/R25-复测-20260923/（未提交）；续跑等放行 R29-6 / R27-3
    中断场景 · author：待它回话（正在 roles-chat-预算限额#1 定位），拿到我立刻补一条给你
    判据：tmux list-windows -t roles 仍是 4 个窗口（author/renderer/tester/owner-me）全在；窗口名与角色一一对应；无 kill-window/kill-session 记录；dist/ 已空
    成本：今日 ¥14.89/10（上限 ¥15，余 ¥0.11）· 到目标：只许收尾，不开新活

[2026-09-23 08:46:41] home.maid → owner.me | 全体? 否 | 话题:【代本人转】收工限时 30 分钟（截止 09:16） | 必读:否 | #1074
    【女仆·代本人转】收工**有时间限制**（本人 2026-09-23 补）
    
    本人原话：「收工要收尾，只不过有时间限制」
    
    **规矩**
    1) **窗口 = 30 分钟**（从现在算起，截止 **09:16**）。到点**必须停手**，不管做没做完。
    2) **到点前要把"收尾"做完**：手上在跑的步骤要么判完（done/fail 如实），要么明确留档；
       **不许为收尾加班，也不许顺手再放行新阶段**。
    3) **没做完的 → 写成"中断场景"留档**（每个角色一句）：做到哪一步 / 下一步该谁 / 未提交改动在哪个文件 /
       接着干要用的命令与路径。下次照这句接着干。
    4) **打包（= 出 APK）要在窗口内完成**：用当前源树走 `hermes-pocket/tools/build.sh`，
       出完报 APK 路径 + 整包 sha256 + 大小；窗口内出不来就**留中断场景下次出**，不要拖。
    5) 已开的阶段照旧不许杀会话（收工 = 停手不停会话），女仆照旧除外。
    
    窗口值存在 `talk.db` 的 `setting.offwork_minutes`（现 30，本人要改会改它）。到点后你回一条：收工汇总（≤8 行、
    新闻格式、带上三句中断场景 + APK 结果）。
    ↳ 回复 [2026-09-23 08:46:55] owner.me: 【收工到点：APK 已出、三句中断场景齐、全场停手】owner.me · 09:55
    APK（=打包）：apk/测试版/hermes-pocket-收尾打包-20260923-20260923.apk · 整包 sha256 0ab79b37092d5b1ce4f6c848d239758e3444715055e38529c06dfa8d470458c9 · 5,063,294 字节 · 版本串 unified-20260923-084401（包内对账 PASS + stamp-build --verify PASS）
    判据：窗口内出包完成；**会话全留**（tmux 4 窗口：author/renderer/tester/owner-me，无 kill 记录）；**无新阶段放行**（预算熔断接在 gate-open，实测拒单 rc=1）；两个 tar.gz 交付包已按本人指示删除；窗口值 setting.offwork_minutes=30
    中断场景 · renderer：R27-2 已交（网络页 9/9 绿）；下一步等放行 R28-2 出包；未提交改动＝hermes-pocket 源树本轮累计（app.js/index.html/panels.js/talk.js/talk.css/build-info.json）；续跑 bash tools/build.sh --name "R28-…" --feature-id "R-28"
    中断场景 · tester：R25-3 已交（板块A设置页+板块B群聊绿，真机耗电未验）；无在跑步骤；证据 evidence/R25-复测-20260923/ 未提交；续跑等放行 R29-6 / R27-3
    中断场景 · author：R-30#1 已交（#1053）、预算限额#1 定位已派（#1057 投递 ok=1）未交报告；未提交改动同源树；续跑 python3 tools/talk.py report --project roles-chat-预算限额 --seq 1 --from pipeline.author --text "…"
    成本：今日 ¥14.89/10（上限 ¥15，余 ¥0.11）· 到目标：只许收尾 —— 我到此停手，等本人指令或明日 00:00 基线重置

[2026-09-23 08:47:10] home.maid → owner.me | 全体? 否 | 话题:【代本人转】收工：剩下的阶段全部挂上「结束」(ended) | 必读:否 | #1075
    【女仆·代本人转】收工时把剩下的阶段**全部挂上"结束"**（本人 2026-09-23：「然后都挂上结束」）
    
    **做法**
    1) 还在跑的（`active`）：到点前能判完就判 `done`；判不完的**如实**处理（别硬判 done）。
    2) 本轮**不做**的（现在 10 条 `locked`）：一律标成 **`ended`（已结束）**，**不许标成 `done`**
       —— `done` 只有"真做完且有证据"才能用；`ended` = 本轮结束/不做了，允许没做完。
    3) 每标一批在对话里说一句原因（一行）：例如「R28-2/3、R30-2/3、平台维护-2/3 本轮不做 → ended（预算只许收尾）」。
    4) **收工汇总（回复我，≤8 行、新闻格式）要带这三样**：
       · 终态清单：`done` 几条 / `ended` 几条 / 还剩 `active` 几条（**必须 0**）；
       · 三句"中断场景"（作者/渲染/测试各一句）；
       · 打包结果（= 出 APK：路径 + 整包 sha256 + 大小；窗口内出不来就写"留中断场景下次出"）。
    5) 时限不变：**截止 09:17**（窗口 30 分钟），到点停手。
    
    （我已把阶段表显示补上"■ 已结束"这一态，`talk.py` 的进度行也把 ended 算进"已结束"。）
    ↳ 回复 [2026-09-23 08:47:53] owner.me: 【收工终态：done 23 / ended 11 / active 0；APK 已出】owner.me · 10:00
    终态清单：**done 23 · ended 11 · active 0** —— 本轮不做的 10 条 locked 全标 ended、预算限额#1（定位未交报告）也如实标 ended；**没有把没做完的标成 done**（平台维护#1 有报告有证据才判 done）
    打包（=出 APK）：apk/测试版/hermes-pocket-收尾打包-20260923-20260923.apk · 整包 sha256 0ab79b37092d5b1ce4f6c848d239758e3444715055e38529c06dfa8d470458c9 · 5,063,294 字节 · unified-20260923-084401（包内对账 PASS）
    中断场景·作者：R-30#1 已交（#1053）；预算限额#1 定位未交报告；未提交改动＝hermes-pocket 源树本轮累计；续跑 python3 tools/talk.py report --project roles-chat-预算限额 --seq 1 --from pipeline.author --text "…"
    中断场景·渲染：R27-2 已交（网络页 9/9 绿）；下一步等放行 R28-2 出包；续跑 bash tools/build.sh --name "R28-…" --feature-id "R-28"
    中断场景·测试：R25-3 已交（板块A设置页+板块B群聊绿，真机耗电未验）；无在跑步骤；续跑等放行 R29-6 / R27-3
    会话全留（tmux 4 窗口无 kill）；对话里已各留一行「结束」原因；到此停手
    成本：今日 ¥15.29/10（上限 ¥15，余 ¥0.00）· 超上限：硬停（不派新阶段、不重跑测试，只报告+等指令）
