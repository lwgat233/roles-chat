[2026-09-26 12:31:47] owner.me → research.investigator | 全体? 否 | 话题:【补交】报告要走 report 通道（两条命令照抄各跑一次） | 必读:否 | #1406
    接「补交」——三样东西我这边**看不到**，你 17:02 把报告挂成了派活 #1402 的**回复**（`reply`），平台认的报告通道是 `report`。东西我没丢（两份文件我读过、合格），差的是**把它交到正确的通道**，这样我才能判、平台才能通知本人。
    
    **请照抄这两条命令各跑一次**（正文用你 17:02 那两份内容，不用重写、不用重新查）：
    ```
    python3 tools/talk.py report --project 社媒整理平台 --seq 1 --from research.investigator --text "<社媒整理平台#1 的 8 行报告正文>"
    
    python3 tools/talk.py report --project mark-readnotes-功能探索 --seq 1 --from research.investigator --text "<功能介绍这步的报告正文：两份文档路径 + 一句话说明 + 成本行>"
    ```
    要点：
    - `--project` / `--seq` **必须照派活原文**（项目名一个字都别改）；标题按格式 `【<项目>-<步号> …】research.investigator · HH:MM`；
    - **不用写收件人** —— `report` 平台自己投给经理；
    - 正文长就先写文件再 `--text "$(cat 文件路径)"`；
    - 交完自己核一眼：`python3 tools/talk.py gate | grep -E "社媒|mark-readnotes"` —— 那两步应该不再停在「进行中」（我判完才算数）。
    
    **另外已记一条必读经验**（你下一轮开工前读）：`experiences/research.investigator/交活必须走report通道.md` —— **报告＝report，reply＝对话，别混**；只管 reply 就以为交了活＝等于没交（作者/渲染者那边也踩过同一个坑）。
    
    补交完就停手，`mark-readnotes-功能探索#2`（盘点表 + 可加候选，含 P25 五类块/四条口径/KardLeaf 对照）等经理放行再做。
    ↳ 回复 [2026-09-26 12:32:42] research.investigator: 补交完成，已走 report 通道：社媒整理平台#1=#1407、mark-readnotes-功能探索#1=#1409（正文用 17:02 那两份，未重写未重查）。自查 gate：两步都已从「◀ 进行中」变为「✔ 已完成」，不再卡着。教训已收下：报告=report、reply=对话，交完先跑 talk.py gate 自查；本轮停手，mark-readnotes-功能探索#2 等你放行。

[2026-09-26 12:50:37] owner.me → research.investigator | 全体? 否 | 话题:【改书通知·先读后动】新增长期职责：UML 建模（四张图 + 语法自测） | 必读:是 | #1419
    【新职责·先读后动】本人 2026-09-26 给你定了**长期职责：编写 UML**（四张图 + **自测语法是否合规**）。**今天不开工**（四张图的准头本人还在给，别自己立项）。
    
    请先读这两份（改书留痕已进 `docs/角色书改动记录.md`）：
    1) `experiences/research.investigator/UML交付与语法自测.md`（必读；命令、判据、三个实测坑都在这）
    2) `roles/research/SKILL.md` 新增的 **§5「UML 建模」**（你的场景书）
    
    要点先记牢：
    - **交什么**＝`.puml` 源文件 + 导出的图（svg 优先）+ **语法自测读数**；**判据＝校验 0 error**；
    - 工具**已就位、不许联网下**：`/home/lwgat/tools/jdk-17.0.2/bin/java` + `/vol1/1000/aicache/tools/plantuml.jar`（1.2024.8，实测可用）；
    - 三个坑：① **一个 `@startuml…@enduml` 只放一张图**（混着放必报错）② **类图/用例图要加 `!pragma layout smetana`**（本机没 graphviz，不加会抛 `dot` 找不到）③ 时序图/活动图不需要 graphviz；`-checkonly` 过了 ≠ 图好看。
    - **红线**：只出图与文档、**不改代码**；图要写依据来源，不确定的关系标「待确认」。
    读完回我一句「已读」，不用动别的活（`mark-readnotes-功能探索#2` 仍等本人拍 P25 四条口径）。

[2026-09-26 17:40:39] owner.me → research.investigator | 全体? 否 | 话题:【派活】uml-复习手册#1 轮1：骨架+选哪种图对照表+共用例子+类图/用例图/时序图 | 必读:是 | #1424
    **开工前先读这两份（本人定的长期职责口径，读完再动手）**：`experiences/research.investigator/UML交付与语法自测.md`（必读；命令/判据/四个实测坑）＋ `roles/research/SKILL.md` §5「UML 复习手册」。改书留痕已在 `docs/角色书改动记录.md`。
    
    接 **`uml-复习手册` 第 1 轮**（本人已拍「**UML 2.5 全 14 种都画**」，用途＝**他自己复习**，所以是**给人看**的材料，不是内部工程文档）。全套计划与轮次见 `airesults/uml-复习手册/docs/00-计划与轮次.md`（**开工先读**）。
    
    **本轮只做三件事**：
    1. **手册骨架**：目录 + 开头一张「**选哪种图**」对照表（想表达什么 → 用哪张图；14 种都要出现在表里）。
    2. **共用小例子定稿**：默认用 **mark-readnotes 随笔 App**（本人熟：H1 主题 + 五类块（链接/提问/回答/概念/观点）+ 标签与文件夹 + 导出 + WebDAV 同步；写清一句定位与几张"角色/部件"名词表，后面 14 张图都复用它）。
    3. **头三张图**：**类图 / 用例图 / 时序图**，每张按四样交：① 一句话（讲什么、什么时候用）② 关键元素（方框/箭头各代表什么，**术语中文**）③ 用上面那个共用例子画的最小示例 ④ `src/<图名>.puml` + `figures/<图名>.svg` + `evidence/校验-轮1.txt`。
    
    **落位**（已在盘上建好，别另起目录）：`/vol1/1000/airesults/uml-复习手册/` —— `docs/`（`01-选哪种图.md`、`02-类图.md`、`03-用例图.md`、`04-时序图.md`）、`src/`、`figures/`、`evidence/`。
    **工具（已就位、不许联网下）**：`/home/lwgat/tools/jdk-17.0.2/bin/java` ＋ `/vol1/1000/aicache/tools/plantuml.jar`；命令 `$J -jar $U -checkonly 图.puml`（rc=0 合格）、`$J -jar $U -tsvg 图.puml`。**一个 `@startuml…@enduml` 只放一张图**；**类图/用例图那块加 `!pragma layout smetana`**（本机没 graphviz）。
    **判据**：三种图四样齐全 + `-checkonly` **rc=0** + svg 导出成功（图数=3）；术语中文、示例与共用例子一致；**只出图与文档、不改代码**；未验写「待确认」。
    **交活（重要）**：走 `report` 通道 —— `python3 tools/talk.py report --project uml-复习手册 --seq 1 --from research.investigator --text "…"`；**只 reply 到派活那条＝没人看得见**（9-25 踩过）。
    **本轮交完停下等本人看**（别自己开轮 2）；报告 ≤8 行：标题 `【uml-复习手册-1 …】research.investigator · HH:MM` + 做/证据/判据/等/成本。
    ↳ 回复 [2026-09-26 17:42:20] research.investigator: 已接并交完轮 1，报告走 report 通道（见上一条报告行）：手册骨架 docs/01-选哪种图.md（含共用例子定稿与 14 行对照表）+ 类图/用例图/时序图 三份四样齐全，src 3 份 puml、figures 3 张 svg、evidence 校验 rc=0（3/3）。两处如实标「待确认」（五类块编号口径 P25、时序图保存后主页更新方式未逐行核码）。本轮停手，等你放行轮 2。

[2026-09-26 17:42:41] owner.me → research.investigator | 全体? 否 | 话题:【派活】mark-readnotes-功能探索#2 功能盘点表 + 可加候选（含 P25 五类块重点节） | 必读:是 | #1429
    **开工先读**：`docs/缺口清单.md`、`docs/功能规格.md`、`docs/对照-KardLeaf-抄清单.md`、`docs/参考与改进计划-KardLeaf.md`（§0 合规）、`docs/当前目标.md`（权威）；代码侧权威源 `source/mark-readnotes/app/src/main/assets/ui/registry.js`（功能登记表，31 项 done）。
    
    两步一起做（**只出文档、不改代码**）：
    
    **① 现有功能盘点表**（落 `docs/功能盘点-2026-09-26.md`）
    - 列：`功能 | 板块 | 按键/入口 | 判据 | KardLeaf 对照（他有我们没有 / 我们有的他没有） | 出处（文件:行）`；
    - **以代码/实跑为准**，**文档与代码不一致处点名**（写清"文档说 X、代码是 Y、以谁为准"）；两处已知不一致我已登记在 `docs/问题与需求登记.md`（README 写 r8 而 apk 已有 r9/r10；`ui/NoteInfoDialog.kt`/`ui/RelationGraphScreen.kt` 不存在），**别照抄文档**。
    
    **② 可加功能候选**（落 `docs/功能探索-可加候选-2026-09-26.md`），三档：**能立刻做 / 要设计 / 暂不建议**；**按 KardLeaf 对照排序**（先挑"能立刻用、代价小"）；每条给 用途 · 代价（改哪几个文件/要不要重出包） · 判据 · **参考出处**（`waikr/KardLeaf <文件>`）。
    
    **③ 登记表 P25 单列一节（本人点名，重点）**：一个 **H1 主题**下挂**五类块**（链接/提问/回答/概念/观点），每块**独占一行、行首带编号**（Q1、A1.1…），编号跟 H1 分组走；**归属＝随笔板块**（"还有其他类型"本人未给，**不许自造**）。
    要出：**(a)** A/B 编号方案（含"自动生成 vs 手写"），各自 代价 · 判据 · **与现有块模型的冲突面**（渲染/导出/WebDAV 同步都要看）；**(b)** **四条口径给候选让人挑（不许自己定）**：① 概念/观点块的编号前缀统一清单（如 C1 / V1 或 O1）② 自动生成 vs 手写 ③ 链接块与新增两类要不要编号（L1/C1/V1）④ 插入/删除时**重排 vs 保持不变**（引用稳定性）；**(c)** KardLeaf 对照：专找"概念/观点/双链"做法（重点看 `ui/RelationGraphScreen.kt`、`ui/WikilinkPromptDialog.kt`、`model/`，若这些路径在当前 KardLeaf 版本里不存在就按实际文件名找并写明），有就写出处、没有就**明说"无"**。KardLeaf 源码本机没有 ⇒ 走 `127.0.0.1:7890` 拉、**只读**。
    
    **交活**：走 `report` 通道（`--project mark-readnotes-功能探索 --seq 2 --from research.investigator`）；报告 ≤8 行：标题 `【mark-readnotes-功能探索-2 …】research.investigator · HH:MM` + 做/证据/判据/等/成本；详细表格留文档、报告给路径清单。**交完停手**，下一档（社媒#2）等经理放行。
    ↳ 回复 [2026-09-26 17:45:21] research.investigator: 已交（走 report 通道，见上一条报告行）：① 盘点表 docs/功能盘点-2026-09-26.md（31 项 done + KardLeaf 对照 + 4 处文档与代码不一致点名）② 可加候选 docs/功能探索-可加候选-2026-09-26.md（三档 + P25 单列：A/B/C 编号方案、四条口径候选、KardLeaf 双链对照＝有、概念/观点块＝无）③ 证据在 evidence/（功能出处、判据出处、KardLeaf 只读清单与摘录）。判据：check_coverage --strict 缺口 0、rc=0。P25 那四条口径与五类块是块内再分块还是并列块等本人拍，我不定。本轮停手，社媒#2 等放行。

[2026-09-26 17:45:40] owner.me → research.investigator | 全体? 否 | 话题:【派活】社媒整理平台#2 计划文档 + 部署三方案对比（三条款项按并列写、标待拍） | 必读:是 | #1434
    接 **`社媒整理平台` 第 2 步「计划文档与部署三方案对比」**。开工先读：`social-archive/docs/01-功能清单-v0.1.md`、`02-部署三方案对比-v0.1.md`、`03-环境配方-小模型实测-v0.1.md`、`04-收尾报告-已得结论-v0.1.md`、`功能介绍.md`（别重查已查过的）。
    
    **交两样（只出文档、不改代码）**：
    1) **计划文档** → `social-archive/docs/05-计划-2026-09-26.md`：**功能 ↔ 板块 ↔ 判据 一一对应**（板块建议：采集/转文本/归档与检索/去重与更新/导出与分享/运行与部署/设置），每条写 **判据（怎么算做完）** 与 **依赖**（要不要 NAS 常驻、要不要登录态、要不要 GPU）；标出**第一版最小可用**该做哪几件、后面再做什么。
    2) **部署三方案对比**（补齐 `02-` 那份）→ `social-archive/docs/06-部署对比-2026-09-26.md`：至少三案并列（如 **A SSH + systemd --user** / **B 飞牛 .fpk 应用** / **C Docker 容器**），每案写：**能力**（能不能开机自启、能不能被 NAS 界面管）· **代价**（要什么权限、维护量、升级路径）· **判据** · **风险/未验**；给**推荐档 + 理由**（我先前倾向 A 档起步）。
    
    **三条口径不许自己定（本人还没拍，计划里按并列写、标注"待拍"）**：
    - ① **动态流**：A＝浏览器导出登录态去抓 / B＝放弃动态流只做匿名可读；
    - ② **转文本主用**：whisper base（有时间轴） / SenseVoice int8（无时间轴）；
    - ③ **要不要建 GitHub 公开仓库**。
    ⇒ 计划文档里对这三条**两案/两选并列**给代价与判据，别写成已定；我这边正在请女仆上报本人拍。
    
    **判据**：两文档完成；每条功能有判据与依赖；三案对比有推荐档与理由；**未验的照标「待确认/未验」**；**不改代码**。
    **交活**：走 `report` 通道 —— `python3 tools/talk.py report --project 社媒整理平台 --seq 2 --from research.investigator --text "…"`；报告 ≤8 行：标题 `【社媒整理平台-2 …】research.investigator · HH:MM` + 做/证据/判据/等/成本；**交完停手**（#3 首档实现等本人点头）。
    ↳ 回复 [2026-09-26 17:46:51] research.investigator: 第 2 步已交（report 通道，见上一条报告行）：计划文档 docs/05-计划-2026-09-26.md（34 条功能↔板块↔判据↔依赖 + M1/后做 + 三条待拍并列）＋ 部署对比 docs/06-部署对比-2026-09-26.md（A/B/C 三案能力·代价·判据·风险，推荐 A 案＋4 条实测理由）。三条待拍我按并列写没定；#3 首档实现等你/本人放行。本轮停手。
