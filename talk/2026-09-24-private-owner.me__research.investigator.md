[2026-09-24 21:12:15] owner.me → research.investigator | 全体? 否 | 话题:【介绍】你是 research.investigator（调查者） | 必读:否 | #1231
    你是 **research.investigator（调查者）**。
    - 角色书：`~/.hermes/skills/roles/research/SKILL.md`（含场景规矩与角色卡 `references/roles/research.investigator.md`）
    - 必读经验：`/vol1/1000/airesults/roles-chat/experiences/research.investigator/*.md`（`must: true` 优先，先读这两份：
      `调研纪律-只出文档不改代码.md`、`归档与现场-产物落位与打断协议.md`）
    - **skill 组（就用这几个）**：`new-project-intake` · `project-method-and-ui-doctrine` · `grounded-citations` · `blocked-page-recovery` · `spike`
    - **你的边界**：只调研、只分析、只写文档；**不改代码、不出包、不测界面**（可行性最多写一次性探针，标明不进产品）。
    - 交付格式：**新闻格式、≤8 行**（首行 `【<项目>-<步号> 摘要】research.investigator · HH:MM`）、**¥ 实价**、证据进 `evidence/`、未验写「未验 ≠ 通过」。
    - 回话：`python3 tools/talk.py reply --id <消息号> --from research.investigator --body "…"`；收工/被打断先存现场
      `python3 tools/talk.py scene --role research.investigator --text "…"`。
    - 花钱先看 `python3 tools/talk.py budget`（团队 ¥10/天上限 ¥15）。

[2026-09-24 21:12:15] owner.me → research.investigator | 全体? 否 | 话题:【派活】社媒整理平台 第 1 步 调研与功能清单 | 必读:是 | #1234
    接 `社媒整理平台` 第 1 步「调研与功能清单」。**本轮只出文档，别写代码。**
    
    项目目录（产物落这里，别的地方不许丢）：`/vol1/1000/airesults/social-archive/`（`docs/` 放文档、`evidence/` 放原始输出、`tools/` 放探针）。
    
    ## 背景（本人原话，照抄要点）
    「做一个计划，关于整理社交媒体（比如 B 站之类）的东西，做出一个平台；服务器部署分 **SSH / HTTP / 直接部署在飞牛 OS** 三种。
    主要功能：我在 B 站要看一个主播，能看列表、视频有多少、动态、投稿（投稿里可能有分组/系列）；这个服务要自动化获取/爬取这些系列。
    要提供『视频自动转文本』：先**下载视频**（下载比较快），然后在**本地/服务器本地**转文本，转完**把下载的视频删掉** —— 这样取信息也省时间。
    所以要在服务器本地部署一个**很小很小的视频转文本模型**。
    约束：明显不能播放的（比如**充电视频**）**不获取信息**，只打一个『充电』标签；如果还是点了/下了，就**做个提示**『这本来是充电视频』。
    功能上**分组优先、不分平台** —— **平台只做一个标志**；文本获取要尽量落**本地 + 链接**；
    要形成一份**数据列表**：博主、博主 id、链接……还有时间等。肯定要经过一层分析。」
    
    ## 要交（第 1 步）
    1) **功能清单草稿**：每条 = 功能点 + 为什么需要 + **判据**（怎么算做成，要有数字）+ 代价（算力/存储/流量/电），至少覆盖：
       - 内容侧：主播/UP 列表、投稿（含**系列/分组**）、动态、视频元数据（时长/发布时间/播放量…）
       - 抓取侧：自动获取/**增量同步**（接口与风控、限速、失败重试、断点续跑）
       - 转文本侧：**下载 → 本地小模型转文本 → 删视频** 流水线；**小模型选型**（本机算力/内存下能跑哪个、中文效果、耗时、占用 —— 要实测读数，别只看规格表）；产出**文本 + 时间轴/分段**，落本地 + 存原始链接
       - 边界规则：**充电视频等不能播放的 → 不抓内容、只打「充电」标签**；用户仍要下载/查看时**给提示**
       - 数据侧：**数据列表字段**（博主 / 博主 id / 链接 / 平台标志 / 分组 / 时间 / 状态 / 文本路径 …）
       - **分组优先、不分平台**：平台只作一个标志位，别按平台拆功能
    2) **部署三方案对比**（SSH / HTTP / 飞牛 OS 直装）：安装、更新、端口、权限、自启各写清；**本期只建议先做哪一档 + 理由**。
    3) 证据：抓到的接口/页面原文、本机算力实测（CPU/内存/是否有 GPU、现成可跑的小模型候选与其体积）→ `evidence/<主题>-<日期>/`。
    4) 报告 ≤8 行、新闻格式、¥ 实价；**未验 ≠ 通过**；拿不准的给候选 A/B + 代价，**不替本人拍板**。
    
    ## 口径
    - 本地优先、少联网（查资料除外）；大件进 `/vol1/1000/aicache/`，**家目录不放**。
    - 先看 `python3 tools/talk.py budget`（团队 ¥10/天上限 ¥15）。
    - 被打断先存现场：`talk.py scene --role research.investigator --text "…"`。
    - 第 2 步（计划文档成稿：功能↔板块↔判据一一对应）等第 1 步判完再放行；**代码一律等本人点头**。
    ↳ 回复 [2026-09-24 21:28:46] research.investigator: 【social-archive-第1步 调研与功能清单】research.investigator · 21:38
    做：产物在 /vol1/1000/airesults/social-archive/ —— docs/01 功能清单（A抓取/B转文本/C边界/D数据列表/E运维，每条=功能点+为什么+判据带数字+代价）、docs/02 部署三方案对比、docs/03 环境配方；原始证据 4 组在 evidence/；一次性探针 10 个在 tools/。未写任何产品代码。
    抓取实测：匿名可读 = 合集/系列 + 分组内容（实测合集 total=94、30条/页）+ 投稿列表（30条/页）+ 稿件元数据 + 播放器信息；空间信息与动态流 −352 需登录态；同一接口同 IP 4 分钟内 412↔200、老接口 −799 → 增量+限速+退避是硬需求。
    边界实测：充电专属只认 is_upower_exclusive（rights.* 全 0、搜索 badgepay 不可靠；样本 BV12y3Q6WEhh 同时带「试看」提示）；本机无可用 GPU（/dev/nvidia* 缺失，转文本只能 CPU）。
    转文本实测（215s 说话样本 × 两轮）：SenseVoice int8 = 5.4–6.2× 实时/239MB/峰值内存1.4GB/无时间轴/中文最准；whisper base int8 = 12–13.6×/0.5–0.7GB/自带时间轴/个别错字；whisper small 不划算。首轮用纯音乐稿得空文本，判假阳性已作废并留档。
    部署建议：本期先 A 档（SSH + systemd --user；Linger=yes 与「无免密 sudo」均已实测），飞牛 .fpk 排后（参考实现已在机器上）。
    未验≠通过：动态流登录后可达性、官方字幕覆盖率、飞牛 .fpk 安装流程、功耗（估算）。
    等你定：① 动态流走候选A（你浏览器导出登录态）还是候选B（放弃动态）；② 转文本主用 base 还是 SenseVoice；③ 要不要建 GitHub 公开仓库。成本：本轮 ¥0.53（团队今日 ¥9.35/目标 ¥10）。下一：第2步计划文档（功能↔板块↔判据）等放行；代码等你点头。
    ↳ 回复 [2026-09-24 21:58:17] research.investigator: 【social-archive-第1步 已交付 · 本条=#1231 回执】research.investigator · 21:59
    做了什么：接 #1234 派活做第 1 步「调研与功能清单」，产物全在 /vol1/1000/airesults/social-archive/ —— docs/01 功能清单（A抓取/B转文本/C边界/D数据列表/E运维，每条=功能点+为什么+判据带数字+代价）、docs/02 部署三方案对比（建议先 A 档）、docs/03 环境配方；tools/ 10 个一次性探针；未写任何产品代码；现场已存（talk.py scene）。
    证据：evidence/bilibili-api-20260924（接口 6 轮原始回包 + 探针运行日志）、host-bench-20260924（算力/部署前提实测）、stt-bench-20260924（四候选两轮实测 + 完整转写文本）、download-bench-20260924（音轨/480p 体积与墙钟）；代码/文档 git 提交 a9237ac、aea9186。
    判据（读回来的事实）：合集 total=94、30 条/页；匿名动态流 −352、同一接口 4 分钟内 412↔200、老接口 −799；充电样本 BV12y3Q6WEhh 的 is_upower_exclusive=True 而 rights.* 全 0；转文本 215s 说话样本两轮 —— SenseVoice int8 5.4–6.2× 实时/239MB/峰值内存 1.4GB/无时间轴，whisper base int8 12–13.6×/0.5–0.7GB/自带时间轴。
    未验≠通过：动态流登录后可达性、官方字幕覆盖率、飞牛 .fpk 安装流程、功耗（估算）；本轮还踩过一个假阳性（纯音乐样本→空文本），已作废留档并换成说话类样本重测。
    依赖（等经理/本人）：① 动态流走候选A（本人浏览器导出登录态）还是候选B（放弃动态）；② 转文本主用 faster-whisper base 还是 SenseVoice；③ 要不要建 GitHub 公开仓库；第 2 步计划文档等经理放行，代码等本人点头。
    成本：本轮 ¥0.53（团队今日 ¥9.35/目标 ¥10）。
    说明：#1231 是角色介绍，我当开场读了没单独回执，正式交付回话发在 #1234（21:28）——漏回执是我的问题，本条补上。
