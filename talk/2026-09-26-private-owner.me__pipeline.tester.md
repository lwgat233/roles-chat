[2026-09-26 12:33:08] owner.me → pipeline.tester | 全体? 否 | 话题:【派活】hermes-pocket-R39 第 3 步 复测（末条不被压 + 回归） | 必读:是 | #1415
    接 hermes-pocket-R39 第 3 步「复测」（真机）。作者定位 #1385、渲染者出包 #1391 都已判过；包＝`apk/测试版/hermes-pocket-R39-末条不被压-20260925.apk`（sha256 `a88a6e96…`，包内 `ui/talk.js 32a24df7…`、`ui/talk.css 09d999e2…`）。
    
    **判据（核心）**：**只滚内层到底**后点**最后一条气泡** → **能开信息窗**（作者判据的原文，别滚外层 `.panel-body` —— 它有 101px 可滚，手指滑只滚内层，这正是上轮误判"滚到底也复现"的根源）。
    **三读数（照抄）**：① 末条气泡下沿 vs 粘性条 `#tk-sayline` 上沿（px）与**重叠量**（应 0）；② `scrollIntoView(block:"nearest")` 后 `elementFromPoint` 命中谁（应是**气泡自己**）；③ `#tk-chat` 的 `padding-bottom` 与 `--sayline-h` 实际值（应随条高走，不是写死）。
    **回归（一块主 + 两块跨，理由要写）**：
    - 主块 **①聊天页-单聊**（本次改动面）；跨块 **②聊天页-群聊**（渲染者把选择器从 `.tk-chat` 改成 `#tk-chat` 就是为了不让群聊那条流多出 68px 空白 —— 必须实测群聊流 `padding-bottom` = 0 且末条不被压）、跨块 **⑧频道页**（R-42 卡片一步进单聊 + 右缘热区不许被带坏）。
    - 另外补上两轮的口径：`document.querySelectorAll("#tk-sayin").length`（上轮怀疑"两套输入框"，作者本机台核实无重复 id，真机核一眼）；**数行时必须限定在可见的 `#tk-sends` 容器**（页里有俩，一个 0 高，跨容器数行会把 1 条数成 2 条）。
    - R-41 那条链顺手复核一条：真机回车能发 + 出自己气泡（这条链上一次刚转绿，别被这轮样式改动碰坏）。
    **边界**：不接主机、**未验 ≠ 通过**（"对方会话条数 +1"照旧记未验）；0 未捕获异常 / 0 console.error；常驻键 ≤3。证据进 `evidence/R39-复测-20260925/`；报告标题【hermes-pocket-R39-3 …】+ 4 行 + 成本¥。
    ↳ 回复 [2026-09-26 12:36:50] pipeline.tester: R39 第 3 步复测交付：#1416（8 行）。**本次测了 3 块**：主块①单聊（改动面）＋跨块②群聊＋跨块⑧频道页；理由＝这轮改的是**共用滚动容器**上的 padding、选择器从 `.tk-chat` 收窄成 `#tk-chat`（群聊流有同一个 class 但没 id），必须实测群聊没被多塞空档；频道页是 R-42 刚挪入口，防被样式改动带坏。
    【核心判据＝绿】**只滚内层 `#tk-chat`**（scrollTop 4655 ≥ 上限 4654，全程没碰外层 `.panel-body`）→ 末条（`tk-bub him`「pipeline.author → 我…」）下沿 **498** vs `#tk-sayline` 上沿 **510** ⇒ **重叠 0px**（间隙 12px）→ **真触摸末条（设备 289,1405）→ 信息窗真开，全名 pipeline.author** ✓
    【三读数照抄】① 末条下沿 498 / sayline 上沿 510 → **重叠 0**（滚到底前后一致）；② `scrollIntoView({block:'nearest'})` 后 `elementFromPoint` **命中气泡自己**（hitSelf=true，没被粘性条/输入框吃掉）；③ `#tk-chat` 的 `padding-bottom` = **68px** ＝ `calc(--sayline-h 60px + --safe-b 0px + 8px)`，`--sayline-h` 实测 **60px**（＝粘性条实测高）⇒ 随条高走、**不是写死**；`--safe-b`=0（模拟器无安全区）。
    【回归】跨块②群聊：`#tk-stream` padding-bottom = **0px**（有 class 无 id ⇒ id 选择器不命中，正是收窄选择器的目的）、末条与 `#composer` 重叠 **0** ✓；跨块⑧频道页：右缘热区 **44×61** 命中 `SPAN.tk-carethit`、点热区→信息窗且仍在频道页、点卡片名字→**一步进单聊**（view=role、输入框在）✓；R-41 链顺手复核：真输入法打字后回车 → **talk.say 1 次** + 状态行「已送达 3329ms」✓。
    【你点名的两条读数】① 真机 `document.querySelectorAll('#tk-sayin').length` = **2**（作者本机台说无重复 id，真机是 2 个）—— 首屏**双份渲染**（气泡 18 = 9×2、输入框 2 个），任一次重画后恢复单份（重画后 10 条气泡）；**不是本轮样式改动引入**（R32/R41 我也读到过 2 个），但取元素要按"最上面那个"取（点得着、能打字的是它；我给错那份赋值时白跑出一次"回车没反应"的假红）；② 数行已**只看可见的 `#tk-sends` 容器**（页里两个，一个 0 高），本次所有行读数都按这条得到。
    【过程如实记一条】第一次跑把"末条"选成了**我自己的气泡**（thread 末项是 me），点它不开窗是**预期**（R-37 B 只给非本人气泡挂点击）；改成末条＝他说的之后即绿。
    0 未捕获异常 / 0 console.error；常驻键 keybar16/composer2；未验≠通过＝不接主机那几条（"对方会话条数 +1"）。证据：evidence/R39-复测-20260925/（结论 2d64e3cad02a + 3 份原始输出 json）。模拟器已停。

[2026-09-26 17:52:11] owner.me → pipeline.tester | 全体? 否 | 话题:【派活】hermes-pocket-R45 第 3 步 复测（真机实体键盘 Shift+Enter；R-41 不许回归） | 必读:是 | #1451
    接 **`hermes-pocket-R45` 第 3 步「复测」**（真机）。作者定位 #1442、渲染者出包 #1447 都已判过。
    **包**：`apk/测试版/hermes-pocket-R45-Shift回车不误发-20260926.apk`（5091338 字节，整包 sha256 `c9615844…`；**我核过**：包内 `ui/talk.js` `7cbe502b…`＝源树逐字节；源码里三处改动都在 —— `shiftAt` 时间戳闩 / keydown `e.key === 'Shift' || e.shiftKey === true` / keyup **只记 true 不清零**）。
    
    **判据（核心，逐条给数字）**：
    1) **实体键盘 Shift+Enter ⇒ 发出 0 条**（`talk.say` / `talk.shout` 计数 0）、**文字仍留在框里**；**紧接着再按普通回车 ⇒ 1 条**（不粘滞）；
    2) **普通回车 ⇒ 1 条**（单聊与群聊各一次）；
    3) **软键盘（Gboard 形状：组字态后回车）⇒ 1 条** —— **R-41 那条链不许回归**（这是本次最容易碰坏的地方，作者七用例里专门保了它）；
    4) **同一次回车不出两条**（三条路同触发的 400ms 闸仍在）；连按两次（间隔 600ms）⇒ 2 条。
    **分板块（照 §10，理由要写）**：主块 **①聊天页-单聊** ＋ **②聊天页-群聊**（同一公共入口 `bindEnterSend`，作者实测两处都中招）＋ 跨块 **⑧频道页**（授权回复框走同一个函数，同构）；另顺手复核 **R-39** 那条（末条气泡不被压：只滚内层到底后点末条能开窗）与 **R-42**（右缘热区）—— 它们跟本改动无关，但都要在同一包里过一眼。
    **读数要求**：`document.querySelectorAll('#tk-sayin').length`（上轮真机读到 2、重画后 1 —— 取元素按**最上面那个**取，且数行只数**可见的 `#tk-sends` 容器**）；0 未捕获异常 / 0 console.error；常驻键 ≤3。
    **边界**：不接主机、**未验 ≠ 通过**（"对方会话条数 +1"照旧记未验）；证据进 `evidence/R45-复测-20260926/`；报告标题【hermes-pocket-R45-3 …】+ 4 行 + 成本¥；**交活走 `report` 通道**（`--project hermes-pocket-R45 --seq 3`）。
