[2026-09-24 20:28:19] owner.me → pipeline.tester | 全体? 否 | 话题:【派活】hermes-pocket-R33 第 3 步 复测（真点+坐标，分板块） | 必读:是 | #1143
    接 hermes-pocket-R33 第 3 步「复测（真点+坐标）」。作者定位 #1130、渲染者出包 #1139 都已判过；别重读大文件，改法与读数在 evidence/R33-定位-20260924/ 与 evidence/R33-实现与出包-20260924/。
    
    产物：apk/测试版/hermes-pocket-R33-触控44dp-20260924.apk（整包 sha256 70247df9…）；改动只有两条样式文件 style.css(262)、talk.css(3/42/46)。
    
    要测（**分板块**，照 SPEC §10：一次一块、单独驱动、单独证据；本改动是**跨板块**的 —— 同一批 CSS 类在多个板块复用，报告里要写明「为什么跨」）：
    1) **主块：设置页** —— 「看明细 / 清理」两个键的真机真触摸（不是注入事件）：点下去要真出明细 / 真弹出清理确认；量坐标（px 与 dp）含大小与上/下沿；
    2) **群聊**：「投递台账」键（原 32px）真点要真展开（判据：明细窗开），喊话行三个键（喊话/广播/身份）真点要真触发；
    3) **单聊 + 频道页**：同款 `.tk-chip` 复用处的行高 +12px 有没有把文案挤折行、有没有遮挡相邻控件（量行高与折行数）；
    4) **常驻按键 ≤3** 与遮挡（顶栏/键条/键盘）照旧量；
    5) 间距如实报（现状 8px/8px/0px 未动）—— **间距是否必须 ≥44dp 我还在请示本人，本条不计入合格判定**，只报读数。
    
    口径：**不接主机、未验 ≠ 通过**；报告标题【hermes-pocket-R33-3 …】+ 4 行 + 成本¥，写清「本次测了哪几个板块」；证据进 evidence/R33-复测-<日期>/。
    按 SPEC §10：报告第一条必须写「本次只测了哪一块/哪几块」，写不清判不合格（返工）。
    ↳ 回复 [2026-09-24 20:33:57] pipeline.tester: R33 第 3 步复测交付：#1151（7 行，标题+导语+细节+成本）。**本次测了 4 块**：设置页(主块) · 群聊 · 单聊 · 频道页；跨测理由＝改的是同一批 CSS 类（.btn/.tk-chip/.tk-askin/.tk-act）在四块复用。
    · 44dp 达标：看明细 71×44、清理 57×44、投递台账 77×44（原 32）、喊话行三件均 44；底边/上沿读数在证据 json 里。
    · 真机真触摸（一律  系统触摸，不是注入事件）5 绿：看明细→明细真出来（230→374 字，出「thread.* 7.8KB」）；投递台账→明细窗真开（内容如实降级「读不到台账：还没连接」）；身份键→本人说变经理说；输入框→获焦并收到 R33shout；广播→真发出「已送达 ≈29ms」且输入框清空。
    · **1 红：清理键点了不弹确认** —— window.confirm 调用 0、无弹窗无确认键，而是直接执行并 toast「清掉 0 个键，释放 0.0KB」（9→9 键）。判据写的是真弹出清理确认，所以判红；要不要加确认请你定。
    · 折行/遮挡：单聊+频道页 15 个控件行框数全=1（+12dp 没挤折行）、控件间 0 相交；发现 1 处**既有遮挡**：频道页「去群聊 →」px(12,595,87×44) 中心被 #composer 盖住（hit 落到 DIV，覆盖链 composer(z=30)）。
    · 间距按你的口径只报读数：8/8/0px（未动，不计入合格判定）；页面常驻键群聊 3≤3；#keybar 功能键条 16 键（既有设计，不在本轮）。
    未验≠通过：接主机那几条（信息窗/气泡/删会话/切会话/开左栏）。另附一条观察交 author：不接主机时广播也显示「已送达 ≈29ms」，是否有假成功风险由他判。
    证据：evidence/R33-复测-20260924/（结论 a93bcfbb0f32 + 5 份原始输出 json + 3 张截图）。模拟器容器已停。

[2026-09-24 20:34:06] owner.me → pipeline.tester | 全体? 否 | 话题:【派活】hermes-pocket-R34 第 3 步 复测（接入频道，分板块） | 必读:是 | #1155
    接 hermes-pocket-R34 第 3 步「复测（真点+坐标）」（作者定位 #1144、渲染者出包 #1149 都已判过；改法/读数在 evidence/R34-定位-20260924/ 与 evidence/R34-实现与出包-20260924/，别重读大文件）。
    
    产物：apk/测试版/hermes-pocket-R34-接入频道-20260924.apk（整包 sha256 5c1556d8…）；改动只有 talk.js 两处（roleCard 505-530、信息窗 823-826）。
    
    要测（**分板块**，照 SPEC §10；本次是跨块，报告里写明「为什么跨」）：
    1) **主块 ⑧ 频道页（角色列表）**：真机看五张角色卡 —— 有频道的卡显示「**接入频道：qqbot**」、无频道的四张卡**不显示该行且高度不变**（60/61px）；量卡片高度（px/dp）与文案有没有被裁；
    2) **跨块 ① 聊天页-单聊**：真机点消息头开**信息窗**（`openRoleSheet`，与频道页共用）—— 键名是「**接入频道**」、有频道显 `qqbot`、无频道显「**未接**」（旧词「能接入 /（无）」应当搜不到）；
    3) 真触摸（`adb shell input tap` 系统触摸，不是注入事件）：点角色卡要真开信息窗；
    4) 遮挡/常驻按键照旧量（顺带复核你上次报的「频道页『去群聊 →』被 #composer 盖住」是否仍在）。
    
    口径：**不接主机、未验 ≠ 通过**；报告标题【hermes-pocket-R34-3 …】+ 4 行 + 成本¥，第一条写「本次只测了哪一块/哪几块」；证据进 evidence/R34-复测-<日期>/。
    ↳ 回复 [2026-09-24 20:40:01] pipeline.tester: R34 第 3 步复测交付：#1167（7 行）。**本次测了 2 块**：主块⑧频道页(角色卡) ＋ 跨块①聊天页-单聊；跨测理由＝roleCard(505-530) 与信息窗(823-826) 读同一份 r.channels，必须两块一起量。
    绿：5 张卡里只有带频道的 pipeline.author(369×79px=78.9dp) 显示「接入频道：qqbot」（class=sub、data-testid=talk-rolechans、文案未裁），无频道四张 59.5/62.7/59.5/62.7dp 都没有该行、没被撑两行；信息窗键序 全名/会话/状态/欠回复/标签/**接入频道**，取值 author=qqbot、tester=**未接**、maid=未接；旧词「能接入」页面 0 次（包内只剩 505 行一处注释）；折叠区仍「频道（2）▸」没被 r.channels 串；旧 .tags/.tk-tag 页面已无。真触摸（adb input tap，设备 540,1057）点卡→信息窗真开（抬头「功能创造者」、接入频道 qqbot），点 ✕ 真关。
    红：**单聊点消息头不开信息窗** —— 真触摸 #tk-sheet 未出现；代码同证：paintChat() 整段 0 个 click 监听、0 处 openRoleSheet（只有 paintStream 给非本人气泡挂了）。要不要补请你定。
    复核：「去群聊 →」现在 px(12,882,87×44) 在视口以下(778，elementFromPoint=null)→需滚动；上一轮那次 y=595 是被 #composer 盖住 —— 两种点不到都记；第 5 张卡 home.maid 中心落在底部固定条带内(hit=false)；常驻键 keybar 16 / composer 2。
    环境插曲：中途 SystemUI ANR 弹窗（'System UI isn't responding'）吃掉全部 adb tap → 首批真触摸全假红；点「Wait」关掉后同批重跑全成功（真手指验收要先查 mCurrentFocus）。
    未验≠通过：不接主机那几条 + 无频道角色的卡未逐个真触摸。证据：evidence/R34-复测-20260924/（结论 383e36d93ef8 + 2 份 json + 2 张截图）。模拟器容器已停。

[2026-09-24 20:40:06] owner.me → pipeline.tester | 全体? 否 | 话题:【派活】hermes-pocket-R35 第 2 步 复测（清理确认框真点） | 必读:是 | #1175
    接 hermes-pocket-R35 第 2 步「复测（真点确认/取消）」。渲染者出包已判过；读数在 evidence/R35-实现与出包-20260924/，别重读大文件。
    
    产物：apk/测试版/hermes-pocket-R35-清理确认-20260924.apk（整包 sha256 4de22562…）；改动＝panels.js 清理键加确认 + cache.js 把清理规则抽成只算不删的 plan()。
    
    要测（照 SPEC §10；本改动只在**设置页**，主块＝设置页，无需跨块）：
    1) 真触摸（`adb shell input tap`）点「清理」→ **确认框真出现**（DOM：`.hp-dialog`，确定 `[data-y]` / 取消 `[data-n]`）；判据写「框出现 + 是哪个键」；
    2) **取消** → 键数与占用**一点不变**（给出前后读数）；
    3) 再点一次 → **确定** → 真清 + 原 toast「清掉 N 个键，释放 X KB」（前后读数差要对得上）；
    4) **负向**：点「看明细」**不该**弹确认框；干净态（0 键）点清理**照样弹**且提示写「0 个键」；
    5) 常驻按键 ≤3 与遮挡照旧；**顺带复核你上轮报的「频道页『去群聊 →』在视口以下需滚动」与「第 5 张卡 home.maid 中心被底部固定条带压住（hit=false）」—— 这两条我已登记 R-38，本次只报读数、不算本步合格判定**。
    口径：不接主机、**未验 ≠ 通过**；报告标题【hermes-pocket-R35-2 …】+ 4 行 + 成本¥，第一条写「本次只测了哪一块」；证据进 evidence/R35-复测-<日期>/。

[2026-09-24 20:45:20] owner.me → pipeline.tester | 全体? 否 | 话题:【派活】hermes-pocket-R36 第 3 步 复测（送达口径负向为主） | 必读:是 | #1190
    接 hermes-pocket-R36 第 3 步「复测」—— 这是**负向用例为主**的一步（作者定位 #1166 判过：以前是假成功；渲染者出包 #1180 已判过）。
    
    产物：apk/测试版/hermes-pocket-R36-送达口径-20260924.apk 整包 sha256 4703cdd8…（包内 talk.js 855ff7a0…；Bridge.kt 编译进 dex）。
    
    要测（照 SPEC §10，分板块；本次跨块并写明理由）：
    1) **主块 ② 聊天页-群聊**：**不接主机**时真机发一条广播 → 状态行必须是「**没送达：还没连接**」（改前这里是「已送达 ≈Nms」＝假成功，**这条就是本轮的红转绿的判据**），且**不出现**「已送达 + 毫秒」；
    2) 接主机时广播 → 应是「**1/2 已送达 149x ms**」这种**带平台 ms** 的读数 + 逐角色 ✓/✗（与台账 `delivery.ms` 对得上就更好）；
    3) **跨块 ① 聊天页-单聊**：真机发一条 —— 平台**没回 delivered** 时应显示中性态「**已发出（未确认）**」且**不写毫秒**；平台明确没投成时应显示「没送达：…」；
    4) 真触摸（`adb shell input tap`）触发以上各条，不是注入事件；截图/DOM 原文进证据；
    5) 常驻按键/遮挡照旧量。
    **理由（跨块）**：两页共用同一套发送状态机（`talk.js:1160-1200`）。
    口径：**不接主机、未验 ≠ 通过**；报告标题【hermes-pocket-R36-3 …】+ 4 行 + 成本¥，第一条写「本次只测了哪几块 + 为什么跨」；证据进 evidence/R36-复测-<日期>/。
    ↳ 回复 [2026-09-24 20:45:28] pipeline.tester: R35 第 2 步复测交付：#1186（8 行）。**本次只测了 1 块：设置页**（本地占用卡的清理键）—— 改动只在这一块（panels.js 单卡 + cache.js 只读 plan()），无需跨块。
    ① 真触摸(adb input tap)点「清理」→  真出现，文案「清理本地缓存？/将清掉 3 个键（约 120KB）/规则：7 天没用过的先删，再把总量压到 2MB 以内」，确定=「清理」、取消=「取消」（各 162×44 设备像素 = 59×44dp）。
    ② 真触摸「取消」→ 4 键/160.2KB **一点不变**，且 toast 里没有清理那条（只有「已同步…」）。
    ③ 再点清理→真触摸「确定」→ **真删**：4→1 键、160.2→40.1KB（差 120.1KB，与 plan() 预览 120.1KB 分毫不差），meta 只剩 probe.today，toast「清掉 3 个键，释放 120KB」原样照旧；卡片文案同步刷成「1 个键 · 40KB」。fixture＝3 个 30 天没用的键 + 1 个今天的（只造数据，判定与删除全走产品代码）。
    ④ 负向：真触摸「看明细」不弹框、无 toast、明细直接出；干净态（clearAll→0 键）点清理**照样弹**且写「将清掉 0 个键」。
    ⑤ 常驻键 keybar 16 / composer 2（既有）；0 未捕获异常 / 0 console.error；先做校准触摸（群聊身份键「本人说」→真触摸后变「经理说」）确认坐标映射对。
    R-38 复核（只报读数）：去群聊 → 这次 px(12,526,87×44) 在视口内且命中自己（上轮 882 在视口外）；home.maid px(12,232,369×63) 命中自己、与 composer(y594-682) 不重叠（上轮 hit=false）——两轮列表形态/滚动位置不同，非回归。
    未验≠通过：不接主机那几条（本轮设置页不涉及）。证据：evidence/R35-复测-20260924/（结论 233290f648bd + 2 份 json + 1 张确认框截图）。
    顺带一条给你：渲染者那条 R35 报告落库的 feature 写成了「hermes-pocket-R35#1-误记」（#1162，topic 虽是 R35#2）—— 不影响我判，但台账口径可能要对一下。模拟器已停。
    ↳ 回复 [2026-09-24 20:45:42] pipeline.tester: 【补正 #1175 回复】上一条里两处类名被 shell 的反引号吃掉了（我的锅），其余数字不变。R35 第 2 步复测交付＝报告 #1186（8 行），**本次只测了 1 块：设置页（本地占用卡的清理键）**，无需跨块。
    要点复述：真触摸点「清理」→ 确认框真出现（类名 hp-dialog，确定=属性 data-y、取消=属性 data-n，各 162×44 设备像素 = 59×44dp），文案「清理本地缓存？/将清掉 3 个键（约 120KB）/规则：7 天没用过的先删，再把总量压到 2MB 以内」；真触摸「取消」→ 4 键/160.2KB 一点不变且无清理 toast；再点确定 → 真删 4→1 键、160.2→40.1KB（差 120.1KB = plan() 预览值），toast「清掉 3 个键，释放 120KB」；负向「看明细」不弹框；干净态 0 键照样弹并写「将清掉 0 个键」；0 未捕获异常。
    证据 evidence/R35-复测-20260924/（结论 233290f648bd + 2 份原始输出 json + 确认框截图）。模拟器已停，我在接 R36。
    ↳ 回复 [2026-09-24 20:49:23] pipeline.tester: R36 第 3 步复测交付：#1193（7 行）。**本次测了 2 块**：主块②聊天页-群聊 ＋ 跨块①聊天页-单聊；跨测理由＝两页共用同一套发送状态机（talk.js send() 1160-1200 + sendStateText/fillSends 一处出文案），R-36 改的就是这套状态机的判成功口径。
    关键手法先说清：**不整体替换 HP.App.rpc**，只包一层，把 talk.shout/talk.say 落回**真 rpc** → 判据 1 跑的是真路径（真 rpc 真抛「还没连接」），不是我拿异常喂出来的。
    ① 不接主机（真触摸「广播」）：状态行「我 → 全体（广播） · 没送达：还没连接」、data-state=failed、带重试键；该行「已送达」0 次、毫秒 0 次（改前是「已送达 ≈Nms」＝假成功）→ 红转绿达成。
    ② 平台回执形状（stub 传输层，ms 取台账真值 8382＝台账 #1190 实测）：「我 → 全体（广播） · 1/2 已送达 8382ms」+ 逐角色「· pipeline.tester ✓ 8382ms」「· pipeline.author ✗ 阶段没放行」(state=partial)。**注意：真连接主机那层＝未验**，我验的是状态机对平台形状回执的处理。
    ③ 单聊：A 回执没有 delivered 字段 → 中性态「已发出（未确认）」(state=unconfirmed、class=wait、无毫秒、**无重试键**)；B delivered=false → 「没送达：阶段没放行」；对照 C delivered=true 缺 ms → 「已送达 ≈1ms」（≈＝界面往返近似态，符合设计注释）。三次都是真触摸。
    ④ 四态同屏（DOM 原文＋截图进证据）；常驻键 keybar 16 / composer 2；#tk-sends y333 h268 在视口内无遮挡；0 未捕获异常 / 0 console.error；先做校准触摸确认坐标映射对。
    台账口径一条：stamp-build --verify 对本包 FAIL，但不是包坏了 —— 源树已盖成 R-37(20:45)，包是 R-36(20:41) 的快照（包内自洽：HP.BUILD = 包内 build-info = unified-20260924-204122）。属"树先走"，要不要加"树冻结"由你定。顺带看到源树 R-37 的 feature 正是「单聊抬头与他说的气泡可点开信息窗」—— 就是我 R34 报的那条红，author 已在 R-37 修。
    证据：evidence/R36-复测-20260924/（结论 191be2df4752 + 原始输出 json + DOM 原文 + 截图）。模拟器已停。

[2026-09-24 20:49:27] owner.me → pipeline.tester | 全体? 否 | 话题:【派活】hermes-pocket-R37 第 3 步 复测（单聊开窗三条负向） | 必读:是 | #1197
    接 hermes-pocket-R37 第 3 步「复测」（真机真触摸）。作者定位 #1179、渲染者出包 #1191 都已判过（读数在 evidence/R37-定位-20260924/ 与 evidence/R37-实现与出包-20260924/）。
    
    产物：apk/测试版/hermes-pocket-R37-单聊开窗-20260924.apk 整包 sha256 1d727e3a…（包内 talk.js 2841fc78…＝源树逐字节）。
    
    要测（照 SPEC §10，**分板块**；跨块要写理由）：
    1) **主块 ① 聊天页-单聊**：
       - 真触摸点**抬头**（`.tk-title`）→ `#tk-sheet` 真开；点**「他说的」气泡**→ 也真开；抬头里的全名与信息窗一致；
       - **三条负向**（这是本轮重点）：点**我自己的气泡**→**不开**；点**空白处**（气泡之间的缝/列表底色）→**不开**；**在气泡上滑一下**（~20–80px）→**不开窗**且**列表照常滚动**（给 scrollTop 前后值）；
       - 点完再滑、滑完再点都正常（不粘滞）；
    2) **跨块 ② 聊天页-群聊**：点非本人气泡仍开窗（原行为没被弄坏）、点本人气泡不开；
       理由：两页共用同一套 `bubbleEl`/`openRoleSheet`。
    3) 常驻按键/遮挡照旧量；顺带只报读数复核 R-38（不计本步判定）。
    口径：**不接主机、未验 ≠ 通过**；报告标题【hermes-pocket-R37-3 …】+ 4 行 + 成本¥，第一条写「本次测了哪几块 + 为什么跨」；证据进 evidence/R37-复测-<日期>/。
    ↳ 回复 [2026-09-24 20:54:10] pipeline.tester: R37 第 3 步复测交付：#1199（7 行）。**本次测了 2 块**：主块①聊天页-单聊 ＋ 跨块②聊天页-群聊；跨测理由＝两页共用同一套 bubbleEl/openRoleSheet（单聊走新加的 tapWho、群聊走原来那条监听），只改单聊挂法必须回头量群聊有没有被带坏。
    ① 主块（真触摸）：点抬头 .tk-title → #tk-sheet 真开、信息窗「全名 pipeline.author」与抬头一致 ✔；点他说的气泡（.tk-bub.him）→ 真开 ✔；三条负向全绿 —— 点我自己的气泡不开 ✔、点空白缝（气泡之间）不开 ✔、在气泡上滑不开窗且列表照常滚动 ✔。
    ② 滑动读数（scrollTop 前后 + 上限）：向下滑 70px 4427→4366、向上滑 70px 4366→4427（回钉底，max=4426，所以"上滑不动"是本来就到底）、滑 40px 4427→4402、滑 20px 4402→4396 —— 四次都不开窗；不粘滞：滑完再点真开（全名 pipeline.author）、点完再滑照常滚动且不开。
    ③ 跨块群聊：63 个气泡，点非本人气泡真开（信息窗全名 pipeline.renderer，与点的那条一致）、点本人气泡不开 —— 原行为没坏。
    ④ 常驻键 keybar 16 / composer 2；0 未捕获异常；先做校准触摸确认坐标映射对。
    R-38 只读复核（按要求不计数）：去群聊 → px(12,882,87×44) 在视口下(778, elementFromPoint=null) 要滚动；home.maid 卡 px(12,588,369×63) hit=false（命中到 TEXTAREA.，被底部输入条盖住）。
    两条驱动侧的坑我记下了并写进证据：①在钉底位置向上滑看不出位移（要向下滑并把 max 记下来）；②复用 querySelector('.tk-bub.him') 拿到的是 DOM 第一条（已滚出屏）→ 误判"滑完再点开不了"，改成只点当前可视区内的元素后全绿。
    未验≠通过：不接主机那几条（删会话/切会话/开左栏）＋群聊 live 追加那条（R-37 C 分支）本轮没单独造。证据：evidence/R37-复测-20260924/（结论 + 2 份原始输出 json + 截图）。模拟器已停。

[2026-09-24 20:57:53] owner.me → pipeline.tester | 全体? 否 | 话题:【派活】hermes-pocket-R38 第 2 步 真机复测（滚到底真点 + 四个数） | 必读:是 | #1215
    接 hermes-pocket-R38 第 2 步（**已改成真机复测**：滚到底真点 + 补四个数）。作者定位 #1211 已判过 —— 关键结论：**本地台滚到底后末卡与「去群聊 →」重叠 0px、都能点**；你前两轮的「被压住(hit=false)」是**没滚到底**量到的（列表中部）。
    
    要交（≤8 行、新闻格式、¥实价）：
    1) **先滚到底再判**（这是新口径，已写进 SPEC §10）：`scrollIntoView` 或把 `.panel-body` 滚到底，报 `scrollTop / scrollHeight / clientHeight` 三个数；然后**真触摸**点**末尾卡（home.maid）**与**「去群聊 →」**，给 `elementFromPoint` 命中谁、有没有重叠像素；
    2) **补四个数**（同一次读数里取，用于排 `--vh` 失配）：`.panel-body` 的 `rect.bottom` / `#composer` 的 `rect.top` / `innerHeight` / `visualViewport.height`（作者本地台对照＝**612 / 612 / 778 / 778**）；若有系统栏/键盘，说明它们对这个差值的影响；
    3) 若滚到底后**真机仍点不到**：把上面 4 个数 + 截图一起交，**判定为 `--vh/--vtop` 失配**（那要改 `#app` 高度口径，不是加 padding 能修的）；若滚到底后能点到 → 明确写「**非缺陷，前两轮为未滚到底**」；
    4) 顺带量：滚到底后末尾到 `#composer` 顶的**间隙 px**（本地台＝24px，即 `.panel-body` 的 `padding-bottom`）。
    口径：主块 **⑧ 频道页**；**不接主机、未验 ≠ 通过**；报告标题【hermes-pocket-R38-2 …】+ 4 行 + 成本¥，第一条写「本次只测了哪一块」；证据进 evidence/R38-复测-<日期>/。

[2026-09-24 20:58:49] owner.me → pipeline.tester | 全体? 否 | 话题:【告知】经理角色卡已定：沈知砚（口径不变） | 必读:否 | #1219
    【告知·不用回】本人 2026-09-24 给经理定了角色卡（经理=沈知砚：外冷、话少、指令感强、对标准要求极高）。经理的口吻会变得更短更直接 —— **但交付口径一条没变**：报告 ≤8 行、新闻格式、结论+数字、¥ 实价、证据进文件、按职责分活、静默纪律、预算与收工纪律照旧。判报告仍只按判据+证据，不合格会打回并写清缺哪一条。你们自己的角色书没变，不用重读别人的；只是下次读经理的派活时按这个风格理解（更短、更硬、更少解释）。
    ↳ 回复 [2026-09-24 21:10:45] pipeline.tester: R38 第 2 步真机复测交付：#1225（8 行）。**本次只测了 1 块：⑧ 频道页（角色列表）**，无跨块。对象说明：R38 未出包 → 装最新可用包 R37（整包 sha256 1d727e3a…），量的是现有布局口径（#app 的 --vh/--vtop 与 .panel-body padding-bottom 24px 自 R37 起没变）。
    ① 滚到底：滚动容器 .panel-body（无 id），scrollTop 356 / scrollHeight 863 / clientHeight 507（可滚 356 → 真到底）。
    ② 滚到底后：末卡 home.maid px(12,232,369×63)、「去群聊 →」px(12,526,87×44)，都在可视区、中心 elementFromPoint 命中自己（SPAN.name / BUTTON.tk-act）、与 #composer 重叠 0px；scrollIntoView(nearest) 复看末卡同上。
    ③ 真触摸两者都真触发：末卡（设备 540,861）→ 信息窗真开（全名 home.maid）；「去群聊 →」（设备 153,1641）→ 可见板块 tab-talk → tab-group、#tk-stream 在、标题「群聊」。
    ④ 四个数（同一次读数）：.panel-body rect.bottom 594 / #composer rect.top 594 / innerHeight 778 / visualViewport.height 778（offsetTop 0、scale 1）⇒ 上沿成对相等、页内可视区＝innerHeight ⇒ **不是 --vh/--vtop 失配**（你的 612/612 与我的 594/594 是同一条关系的内容差，不是 inset 差）。
    ⑤ 系统栏/键盘：屏幕 1080×2340、WebView [0,136][1080,2274] → 状态栏 ≈49.5 CSS、导航栏 ≈24 CSS，**都在 WebView 之外**，mInputShown=false → 4 个数里没有系统栏/键盘造成的差额。滚到底后末尾（容器最后一项下沿 570）到 composer 顶 594 的间隙 = **24px**，＝你本地台 24px，＝ .panel-body 的 padding-bottom。
    ⑥ 判定：**非缺陷 —— 前两轮 hit=false 是"没滚到底"量到的**（未滚底时末卡 y=588 落在固定层带内、与 composer 重叠 57px、命中 TEXTAREA；去群聊 y=882 在视口外）。常驻键 keybar16/composer2、0 未捕获异常。
    ⑦ 留你定两条：间隙 24px 未达 §10 的 ≥44dp（作者已给 style.css:223 一行改法）要不要抬；本轮我按"只报读数"记。未验≠通过＝不接主机那几条。证据：evidence/R38-复测-20260924/（结论 + 2 份原始输出 json + 截图）。模拟器已停。

[2026-09-24 21:15:16] owner.me → pipeline.tester | 全体? 否 | 话题:【告知】经理职责七条：角色规范与组内互通 | 必读:否 | #1239
    【告知·不用回】本人 2026-09-24 重申强化**经理职责七条**（平台文档 §⑮，已写进经理角色书 §2.5）。跟你们直接相关的是两条：
    ① **角色规范持续维护**：你们的「该做什么 / 不该做什么 / 交付格式 / 判据」由我写清并随时更新 —— **跑偏先修书、再谈追究**；
    ② **鼓励组内直接沟通、主动上报问题**：pipeline 层可以直接对话（**优先写文件对接**，把路径+哈希给对方更省 token）；**发现缺陷/风险要报、不许瞒**（报了我会记账）。
    你们的角色书与必读经验**没变**，不用重读；只是知道这两条在跑。

[2026-09-24 21:19:04] owner.me → pipeline.tester | 全体? 否 | 话题:【派活】hermes-pocket-R32 第 3 步 复测（30s 0 报错 + live 追加回归） | 必读:是 | #1252
    接 hermes-pocket-R32 第 3 步「复测」。作者定位 #1237、渲染者出包 #1248 都已判过（读数在 evidence/R32-定位-20260924/ 与 evidence/R32-实现与出包-20260924/）。
    
    产物：`apk/测试版/hermes-pocket-R32-死调用清理-20260924.apk` 整包 sha256 `a784e134…`（包内 `talk.js 6873a33f…`＝源树逐字节）。改动＝**删**三处 `pullRoleOutput` 死调用 + 一段 `this.live` 死分支（没有新增功能）。
    
    要测（照 SPEC §10，分板块；跨块要写理由）：
    1) **主块 ① 聊天页-单聊**（主判据）：真机打开单聊页，**连续 30s** 挂着不动 —— **pageerror / console.error 必须 0 条**（改前 30s 抛 12 条、间隔 2500ms；这是本条的**红转绿判据**）；同时报 `talk.since` 调用次数（应约 12 次＝2.5s 照跑）；
    2) **回归（很重要）**：同一 30s 内 —— 气泡仍随轮询刷新、**点非本人气泡仍能开信息窗**（R-37 的 A/B 不能被这次删除弄坏）、发完一条消息仍出终态（「已送达 Nms」或「已发出（未确认）」）；
    3) **新增检查（渲染者点名的一条）**：渲染者说 **R-37 的 C 项（live 追加的气泡挂点按）就写在被删那段里** —— 请你**真机验一条**：页面开着时来一条新消息（live 追加的那批气泡），**点它能不能开信息窗**。
       - 能开 → 一句「R-37 C 项未受影响」；
       - **不能开 → 记为红**并写清（那说明这条回归是真的，我会重开一步给渲染者补回）；
    4) **跨块 ② 聊天页-群聊**（理由：两页共用同一个 `this.timer`/`tick()`）：群聊页 6s 挂机 0 报错 + `talk.since` 次数；
    5) 常驻按键/遮挡照旧量。
    
    口径：**不接主机、未验 ≠ 通过**；报告标题【hermes-pocket-R32-3 …】+ 4 行 + 成本¥，第一条写「本次测了哪几块 + 为什么跨」；证据进 `evidence/R32-复测-<日期>/`。
