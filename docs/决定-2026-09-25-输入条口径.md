# 决定 2026-09-25 · 议题：频道页那片输入条（输入条口径）

**流程**：⑥（本人不在/不拍板时）—— pipeline 层文件讨论 → 经理写决定书 → 三人一人一票、**经理只记票不投票** → 结论通知本人（本人可推翻；推翻写进同一份，不删旧结论）。
**由来**：本人 2026-09-24 原话「我在频道里沟通的时候，那个输入框一直保留在那里，就不太合理。如果你保留在那里……我可以使用它，把这段话发送给频道里的某个角色」；
他的**倾向＝A**（非本场景不保留输入条），但**明确不拍板**：「明天让他们自己去讨论哪个更加合理」。同片输入区的三个症状＝**R-41**（回车不发送，已修）· **R-42**（点频道不能聊天）· **R-44**（这条输入条该不该留）。

## 一、议题

`#composer` 那条输入条（语义＝**写进 tmux 终端**，`app.js:653`）在「频道 / 角色列表」这类**不是终端**的场景里**该不该保留**：
- **候选 A**：不该出现的场景（tmux 会话页以外）**不保留**输入条；
- **候选 B**：**保留，但必须真能用** —— 能**指定发给频道里的某个角色**（收件人显式 + 走 `talk.say` 链路）。

## 二、三人各自的理由与证据（原文文件，逐份落盘）

| 角色 | 票 | 一句话理由 | 文件（原文，可复核） |
|---|---|---|---|
| `pipeline.author` | **B** | 本人的重心是「要么能用、要么别摆」；A 把"能用"一起砍掉；B 只要把收件人做成**看得见的状态 + 明确链路（`talk.say`）+ 与实时模式互斥**，就同时满足"不误导"和"能用"（可拆两步先做收件人指示与路由） | `hermes-pocket/docs/讨论-输入条口径-author.md`（7588B，09:34） |
| `pipeline.tester` | **B** | 只有 B 能定出**可复核的判据**（真机点 + 台账证明"收件人真是我选的那个"、对方条数 +1）；A 案只能验"没出现"，验不了本人那句"我可以使用它"（若选 A，须先把"该出现场景清单"写进规范，否则测试者只能给读数、给不出合格判定） | `hermes-pocket/docs/讨论-输入条口径-tester.md`（5455B，09:31） |
| `pipeline.renderer` | **A** | `#composer` 语义就是"写进 tmux 终端"，放在频道页只会让人误以为能聊天；"发给频道里某个角色"**已由单聊输入条提供**（`talk.js:1095-1105`），A 是把假象去掉、又不新增第二条同义通道，**代价最小**（只动 `app.js` 一处显隐 + 一次 resize 复验）；**B 会在同一个框里混装两种投递语义，直接撞「常驻键 ≤3 / 不写说明文字」两条铁规** | `hermes-pocket/docs/讨论-输入条口径-renderer.md`（6678B，09:33） |

## 三、票数与结论

- **票数：B 两票（author、tester）／ A 一票（renderer）** —— 无平票，按多数。
- **结论：走 B** —— **保留该输入条，但必须先做成"收件人显式"的真输入条**（可见的收件人状态 + 明确走 `talk.say` + 与实时模式互斥），**否则不予保留**。
- **生效范围**：① 频道页（角色列表）本片输入区；② 其它"非 tmux 会话页"场景照同一口径（要么做成能用的，要么不出现）；③ **不影响**已完成的 R-41（回车发送，单聊/群聊/授权框三处真机转绿）与 R-40（会话编号与身份）。
- **执行约束（记 renderer 的反对意见，落实现时必须守）**：实现**不许**破坏「常驻键 ≤3」与「不写说明文字」两条铁规；**不许**新增第二条同义输入通道（单聊输入条仍是"点人说话"的主路）；实现前先给**判据**（真机点 + 台账证明"收件人＝我选的那个人"+ 对方会话条数 +1）。
- **与 R-42 的关系**：R-42 的 ① 修法（**角色卡单击＝直接进单聊**，信息窗挪到卡片右侧箭头）**先做**，让"从频道页到能说话"从 2 步变 1 步；B 案的"频道页自带输入条"**排在它之后**（等本条决定生效后再排期）。

## 四、通知与可推翻

- 本结论**已通知本人**（经女仆转达，2026-09-25）；**本人可推翻** —— 推翻时在本文件追加一节（写明推翻理由与生效时间），**不删上面的旧结论与旧票**。
- 本人 2026-09-24 的**倾向 A** 照原样留在登记表 R-44 一格与本文件（不是"少数派被抹掉"）。

**署名**：`owner.me`（经理 · 沈知砚）—— 只记票、执行、署名，**未投票**。
