# 总结 2026-09-23（收工）—— 事故与规矩

> 依据：本人 2026-09-23 口述（女仆代转）+ 各角色报告与台账。**一处一行**，每条都指到文件/行或台账编号。
> 本文件是总汇；分角色的必读经验见 `experiences/<角色全名>/`（下表"写进哪"一列）。

## A. 事故与缺陷（现象 → 根因 → 修法 → 判据/证据 → 写进哪）

| # | 现象 | 根因（指到链路/行） | 修法 | 判据 / 证据 | 写进哪 |
|---|---|---|---|---|---|
| 1 | 投递后文字「躺在对方输入框里没提交」：经理空转、角色收不到指令 | `deliver()` 多行分支「粘贴 → 0.25s → 回车」，TUI 把紧跟粘贴的回车吞进粘贴区 | 改「粘贴 → 0.8s → 回车 → 0.6s 再补一次回车」（空输入框再回车无害）；并以**对方会话消息条数 +1** 为硬证据复核 | `talk.py` `deliver()` 注释与实测；投递台账 `delivery.ok=1` + 会话条数 | `delivery-and-relay.md`（技能）/ 本人必读经验（收工纪律） |
| 2 | **报告没送进经理会话** → 阶段卡死、角色按静默纪律干等（本人问「怎么都没干活」） | 报告是 `default`、`to_role=NULL`：`relay_targets()` 兜底名单**排除 owner.me** → 报告有"候选"没"收件人" | 报告强制加 `owner.me` 收件人（`relay_once`）；`report()` 直接投一次经理并记 `delivery` | `delivery` 里报告那行对 owner.me = `ok=1`（#863 已是 ok=1） | `experiences/owner.me/报告一到就判-链条不断档.md` |
| 3 | **追问刷屏**：整夜 80 条【卡住】，还会自我繁殖 | `overdue_deliveries()` 只排除 me/owner.me；追女仆、追 `【卡住】` 类消息（转告自己变新待回项）、到上限后每轮重报 | ① 只追「派活/放行/授权/选择」四类（`NUDGE_SKIP_TOPIC` + 控制类前缀）；② 排除 `home.maid`；③ 升级账 `nudge:<msg>:<role>:esc` 只报一次并合并 | `talk.py nudge --dry` 输出为空；SPEC §「逾期追问」 | `SPEC.md`（逾期追问节）/ 技能 `multi-role-orchestration` |
| 4 | **女仆身份/权限搞错**：以为她是普通角色，想给她开窗口/投递 | 没定清「女仆＝本人的 QQ 通道」，控制类命令身份用错 | 女仆不开窗口、不投递；控制类命令一律 `--by owner.me`；`spawn` 对 home.maid 直接拒 | `spawn home.maid` 被拒；`deliver home.maid` 走 QQ 通道 | `experiences/home.maid/会话就是本人的QQ通道-别开窗口.md`、`只能给经理派任务.md` |
| 5 | **预算口径错三次**：① 用「美元估算 × 7.2」；② `setting.budget_base:*` 留美元死数据（读它复算差 8.5 倍）；③ `rollover` 同日重打把当日清零（实测 ¥15.09→¥0.00，熔断失效） | ① 没有价目表；② 基线真身在 `pref` 表，setting 那几条是遗留；③ 重打逻辑把"现在"当成"今天 00:00" | ① 改**人民币实价**（`tools/prices_cny.json` + 高峰/空闲）；② 那条遗留改名 `budget_base_usd_legacy:*` 并留 note；③ 拟「同日不重打，除显式 `--force`」（下一窗口第一件） | `talk.py budget --json` 与手工复算**差 ¥0.0000**（作者实测）；证据 `evidence/预算限额-定位-20260923/` | `experiences/owner.me/预算限额-团队每天10元上限15元.md` |
| 6 | 自检把「不该送到的活」粘进**线上**角色窗口，打断它一个回合 | `talk_selftest` 拿线上角色试"投递被卡"，闸门不拦时就真粘进去 | 自检投递拦截改用**专属角色** `pipeline.auditor`（无窗口）；自检跑前停中转站、跑后起回来；自检假报告不推经理 | 自检 43/43 绿且线上窗口无粘入 | `evidence/selftest-20260923-*.txt`、技能 §7 |
| 7 | 老库缺列 → `send`/`gate-open` 整体报 `table msg has no column named hidden`（放行通知发不出去） | 补列只在 `ensure_schema()` 里，而 `send` 这条高频路径不会先跑它 | 把 `scope/hidden/bind` 三条 `ALTER` 挪进**连接初始化** | 放行通知恢复投递（#723/#724 ok=1）；自检 43/43 | `SPEC.md` / 技能 §7 |
| 8 | 闸门自锁：同一人担任两步时，后一步 `locked` 把前一步的**放行通知**也挡掉 | `blocked_reason()` 写成「任意一个 locked 就拦」 | 改成「**全部 locked 才拦**；有 active 就放行」；自检补回归判据 | 自检 43/43（含该回归）；`delivery` 682/684 与修复后 #709 对照 | `experiences/owner.me/接活先排阶段-派完盯回复.md`、技能 §7 |

## B. 今天定下的规矩（落到哪 → 判据）

| # | 规矩 | 落到哪 | 判据 |
|---|---|---|---|
| 1 | 按职责分活：功能+配方=author、画界面+**出包=renderer**、真点复测=tester | `docs/决定-2026-09-23-按职责分活.md`、`experiences/owner.me/按职责分活-出包归渲染者.md` | `gate` 里界面/出包那步 role=renderer；交付链 author→renderer→tester 不缺环 |
| 2 | 静默纪律：没派活不许动，要动先【申请】且经理认可 | `experiences/*/静默纪律-没派活不许动.md` | 无 active 阶段却发起动作 = 违规；申请有【批准】记录才动 |
| 3 | 周期询问：60 分钟一轮，只问三句（完成怎样/卡在哪/要什么），问答进对话 | `docs/需求-周期询问与静默纪律.md` ① | 一个周期内对话里能查到"问"的记录；没回按 nudge 追问 |
| 4 | ②文件对接：pipeline 层可直接对话，但大段内容走文件（路径+哈希） | 同上 ② | 交接物能在文件里对上；对话只留结论 |
| 5 | ③聊天实时：不放批量补 | 同上 ③ | 事件产生到面板可见的延迟可测 |
| 6 | ⑤总目标三角（省流量/省电/方便），冲突**给设置项**不自己拍 | `hermes-pocket/docs/当前目标.md` §0.5 | 每条交付写明三角取向；冲突项带可切换设置 |
| 7 | ⑥本人不在：文件讨论 → 决定书 → 三人投票，经理不投票只记票 | `docs/需求-周期询问与静默纪律.md` ⑥、`docs/决定-*.md` | 改了方向必须能拿出决定书（议题/票/结论齐） |
| 8 | ⑦省 token：结论+数字，大段走文件，不回放日志 | `experiences/*/省token与每次任务记token.md` | 报告里有成本行；对话无大段回放 |
| 9 | 报告 ≤8 行 + **新闻格式**（标题→导语→细节→成本） | `SPEC.md` §7（一处权威）、五份必读经验 | `report()` 强校验：首行【】标题 + 成本行 + ≤8 行，43/43 自检 |
| 10 | 成本用 **¥ 人民币实价**（价目表 + 高峰/空闲），不乘汇率 | `tools/prices_cny.json`、`talk.py budget` | `budget --json` 与手工复算差 ≤¥0.01（实测 0.0000） |
| 11 | 分板块测试：一次一块 + 坐标/键位是硬读数（≥44dp 为线） | `SPEC.md` §10、`experiences/pipeline.tester/分板块测试-坐标与键位.md` | 报告写明"本次板块"+坐标/键位读数；每块独立驱动与证据 |
| 12 | **打包＝出 APK**（`build.sh`），tar.gz 交付包只在本人明说时做 | 五份必读经验（术语节） | 报 APK 路径 + 整包 sha256 + 大小；包内对账 PASS |
| 13 | 全局预算：**一份池子 ¥10/天目标、¥15/天硬上限**（全部会话），到线熔断 | `experiences/owner.me/预算限额-…md`、`talk.py budget_gate()` | `budget` 输出状态；到 ¥10 后 `gate-open` 被拒（实测 rc=1） |
| 14 | 收工限时 + 保留中断场景 + 剩下的都挂 `ended`（不许拿 ended 当 done） | `experiences/owner.me/收工纪律-留中断场景.md`、`setting.offwork_minutes=30` | 会话全留（无 kill）；每人一句中断场景；终态 active=0、未做完=ended |
| 15 | ⑧编角色书：四样留痕（依据/通知重读/记账/红线） | `docs/角色书改动记录.md`、`SPEC.md` | 改动记录每条对得上本人原话或编号；权限/守卫不许经理自改 |

## 终态

- 阶段：**done 24 · ended 10 · active 0**（本文件生成后又判了 `预算限额#1` → done 24）。
- 打包（=出 APK）：`apk/测试版/hermes-pocket-收尾打包-20260923-20260923.apk` · sha256 `0ab79b37092d5b1ce4f6c848d239758e3444715055e38529c06dfa8d470458c9` · 5,063,294 字节。
- 预算：当日 ¥15.42 / 上限 ¥15 → **硬停**（只报告+等指令）。
