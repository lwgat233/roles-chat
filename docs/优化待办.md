# 优化待办（本人 2026-09-24 定：中午没指示时，经理接着做"昨天的任务"**或未完成的优化**）

用法：中午 12:00 检查发现本人没给指示 → 经理从这里挑**最靠前的一条**接着做（做完把状态改成 done 并补证据路径）。
钱：平台类优化走**团队池**（计入 ¥10/¥15）；**计划流程优化**走**计划结余**（`person/data/plan_budget.csv` 的结存，不计入 ¥10）。

| # | 优化项 | 为什么 | 状态 | 判据 |
|---|---|---|---|---|
| O-1 | 投递"没确认成功"要**落台账** | 现在的多行投递会重试 6 次，但**没记结果** —— 出问题查不出来 | **done**（2026-09-24，证据 `evidence/O-1-投递确认落台账-20260924/原始输出.txt`） | `delivery` 表能看到每条投递的确认结果（成功/重试几次/最终失败） |
| O-2 | 追问/卡住**记账** | 追问已改成"只报一次 + 合并"，但台账里查不到"追过谁、几次" | 待做 | 有一张表或一组行能列出追问历史（msg/role/次数/时间） |
| O-3 | 预算熔断**拒单落库** | 现在到上限只有报错文案，复测算不出"拒过单" | 待做 | 到 ¥10/¥15 时新阶段派不出去，且库里留下拒单记录（作者 #1083 提过） |
| O-4 | **低键位收口** | tester 报过 42px/40px/32px 低于 44dp 的控件，没定论 | 待做 | 要么改到 ≥44dp，要么在 SPEC 里写明"这三个例外可以低于 44dp 及理由" |
| O-5 | 分板块测试**落到 SPEC** | tester 已按板块跑，SPEC §10 还只是口头 | 待做 | SPEC 里有板块清单 + 一板块一驱动的硬规矩 |
| O-6 | 面板 `roles-json` 的 **channels 展示**核对 | 女仆身份已登记（channels=[qqbot]），面板是否真显示没核 | 待做 | 面板里能看到"接入频道"一栏且有值 |
| O-7 | **计划流程优化**（走计划结余） | 每天 ¥2 的结存就是为这个存的 | 待做 | ①计划模板迭代（按本人反馈改，改一次记一条）②mydoc 大文件按块读、别整读 ③进度回写 `study_progress.csv` 的核对 |
| O-8 | 计划任务**自检** | 07:00 那份计划还没跑过第一版 | 待做 | 第一份出来之后核：格式/时间轴/学习块三样齐不齐，并记一条改进 |
