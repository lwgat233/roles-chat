---
name: 交报告必须按派活里写的「项目#步号」填 feature，别自己数
must: true
tags: [渲染者, 报告, feature, 阶段闸门, 返工]
when: 每次跑 talk.py report 之前
verified: 2026-09-24
---

**踩过的坑（2026-09-24 R35）**：派活写的是「hermes-pocket-R35 第 1 步」，我却把报告记成 `hermes-pocket-R35#2`
（那一步是测试者的复测）—— 闸门按 `feature` 精确匹配，经理 `gate-done` 报「第 1 步还没有合格报告」，
整条链卡住，得原样重交一次（#1164 才对上）。

- **feature 照抄派活里的「项目#步号」**（如 `hermes-pocket-R35#1`），**别按"这是我这轮的第二件事"自己数**。
- 交完**自查一句**：`sqlite3 talk.db "select feature from msg where id=<你的报告号>"` 里的 feature 必须与派活一致；
  若交错了，**原样重交**（正文复制一份即可），别去改历史那条。
- 报告正文照旧按新闻格式（SPEC §7）：标题【项目-步号 摘要】+ 导语 + 细节 + `成本：…≈¥W`，**整条 ≤8 行**
  （超行 `report()` 直接拒 —— 已踩过两次）。
