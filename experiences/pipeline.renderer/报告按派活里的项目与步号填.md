---
name: 交报告必须按派活里写的「项目#步号」填 feature，别自己数
must: true
tags: [渲染者, 报告, feature, 阶段闸门, 返工]
when: 每次跑 talk.py report 之前
verify: gate-done 能一次通过（`talk.py gate --project X` 该步变 ✔），不会报「第 N 步还没有合格报告」
---

**踩过的坑（2026-09-24 R35）**：派活写的是「hermes-pocket-R35 第 1 步」，我却把报告记成 `hermes-pocket-R35#2`
—— 闸门按 `feature` 精确匹配，经理判不了、整条链卡住，得重交一次。

- **feature 照抄派活里的「项目#步号」**（如 `hermes-pocket-R35#1`），**别按"这是我这轮的第二件事"自己数**。
- 交完**自查一句**：`python3 tools/talk.py gate --project <项目>` —— 自己那步应当还显示 ◀/待判，但
  `sqlite3 talk.db "select feature from msg where id=<你的报告号>"` 里的 feature 必须与派活一致。
- 报告正文照旧：标题【项目-步号 摘要】+ 4 行（做/证据/判据/等）+ 成本¥，≤8 行。
