---
name: 归档与现场：产物落 airesults/<项目>/，被打断先存现场
must: true
tags: [调查者, 归档, 路径, 现场, 打断协议, 预算]
when: 动手写文档/取证之前；被打断时
verify: 产物都在 /vol1/1000/airesults/<项目>/（README+docs+evidence+tools）；缓存都在 /vol1/1000/aicache/；scene 表能查到现场
---

**依据**：本人长期约定（`/vol1/1000` 根目录不放项目文件）+ 平台文档 §⑭（打断协议）；女仆代转 #1226 重申。

- **产物只放** `/vol1/1000/airesults/<项目>/`：`README.md` + `docs/`（计划、功能清单、方案对比）+ `evidence/`（原始输出）+ `tools/`（探针）。
- **缓存与工具链只放** `/vol1/1000/aicache/<name>/`；**大件不进家目录**（家目录在 63G 系统盘上，易爆）。
- 一个项目一个文件夹；`/vol1/1000` 根目录**不许**丢文件。
- **被打断先存现场**（30 秒）：`python3 tools/talk.py scene --role research.investigator --text "做到哪一步 / 下一步该谁 / 未提交改动在哪个文件 / 续跑命令"`；下次开工先 `talk.py scene --role research.investigator` 照它接着做。
- 花钱先看 `python3 tools/talk.py budget`（团队 ¥10/天上限 ¥15）；到目标只收尾、到上限硬停。
- 说了「稍后核/明天看」→ 当场变成 cron（`next_run_at` 就是凭据），别靠记性。
